# 使用 mahjong_graphic 上游牌面计划

日期：2026-09-08。此计划在实施前写入；现已完成前端实现、本地与服务器测试、公网发布及浏览器验收，结果见 `UPSTREAM_TILE_ASSETS_TEST_REPORT.md`。

## 用户最新决定

停止用户截图及其紧凑图集方案，改用 https://github.com/lietxia/mahjong_graphic 的现成牌面。先前截图相关文档仅为历史记录，不再代表当前方向。

## 选材与实现范围

- 固定上游提交 `3e275804ff58325306710bef3a7406860444bc6a`，选 `Vectors 矢量图/SVG` 带浅色牌底的完整 SVG：34 种普通牌，以及 `0m/0p/0s` 原生红五。
- 素材原文与校验清单入库，保留 LICENSE、来源与提交信息；不截取仓库预览图，不使用用户截图，不重绘图案或加数字角标。
- 构建时仅排列原始 SVG 为单一矢量图集，隔离每张 SVG 的样式/ID，保持原始 path、颜色、比例。无 GitHub 运行时请求，无字体依赖。
- 明确上游字牌编号：1z 东、2z 南、3z 西、4z 北、5z 白、6z 发、7z 中；游戏编号为东南西北中发白。红五直接用上游 0m/0p/0s，不再截图着色。
- 保留共享 `svg.mahjong-art`、`data-face`、60×84 外部尺寸、无障碍名字、暗牌隐私以及所有游戏行为。
- 截图源、旧图集和脚本移到历史归档，不再作为 src 构建依赖；相关测试替换为上游资源验证，不删业务断言。
- 本次只涉及前端牌面和相应构建/测试/文档，不变更规则、后端、数据库、FRP 或 Nginx。

## 验证与交付

1. 验证 37 张源文件 SHA-256、许可证、完整映射、安全 SVG 标签、图集可重建，禁止脚本、外链、字体、图片嵌套。
2. 测试 34 种牌、三张红五、字牌顺序、非法牌、可访问性、暗牌不泄漏；保留手牌、牌河、副露、结算、复盘和选牌编辑回归。
3. 完整前端测试与类型检查/生产构建；确认产物不引用任何旧截图资源。
4. 浏览器查看完整牌面目录，重点检查七条/七筒、字牌、红五、横置和叠牌，以及手机尺寸。
5. 备份远端当前前端后发布新静态资源，再切换入口；检查公网访问、资源内容及后端 PID 不变。
6. 在 `UPSTREAM_TILE_ASSETS_TEST_REPORT.md` 记录实际结果与范围，不把牌面回归称为完整麻将规则重测。
