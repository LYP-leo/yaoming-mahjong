# GitHub 工程同步检查报告（2026-09-15）

## 账号与目标

- 认证接口确认账号：`LYP-leo`，ID `50077176`。
- 目标：`https://github.com/LYP-leo/yaoming-mahjong`，私有仓库，账号具有写权限。
- 初次同步前 Git refs 接口返回空仓库状态；采用已授权的连接器建立源码快照，不强制覆盖远端更新。

## 本地验证结果

| 检查 | 本次结果 |
| --- | --- |
| 前端测试 | 37 个文件、902 项测试通过；命令耗时 23.02 秒 |
| 前端正式构建 | 牌面原始文件完整性、TypeScript、Vite 全部通过；13.71 秒 |
| 后端测试 | 31 个测试类、588 项通过；0 失败、0 错误、0 跳过；34.342 秒 |
| Git 格式检查 | `git diff --check` 和暂存区检查通过 |
| 上传候选审计 | 源码/文档/素材均为有效 UTF-8；没有真实凭据命中 |
| 忽略规则 | 房间数据、依赖、构建产物、旧截图、归档及训练模型不在候选中 |
| 牌面素材 | 保留用户选定上游 SVG、原始文件和许可；最大素材约 250 KB |

上传内容包含服务器地址和本机路径等部署说明，所以目标保持私有；没有上传 SSH/FRP 密码、环境秘密或在线房间身份。

## 快照与远端验收

- `scripts/github-sync-snapshot.mjs` 按已有 `.gitattributes` 取得规范化内容、Git blob SHA、模式及完整清单。
- 本机生成的清单和上传回执保存在被忽略的 `artifacts/github-sync-20260915/`，不会混入源码。
- 分支更新后必须读取远端递归树，逐文件核对路径、模式、blob SHA；若不一致不能视为同步完成。
- 最终提交 SHA 与远端逐文件核对结果写入本机上传回执，并在交付消息中给出。报告本身不嵌入自己的提交 SHA，避免自引用改变文件哈希。
- 本地原有 HEAD 和暂存区保持不变；连接器快照提交不宣称已通过本机 Git 推送，也不伪造旧提交历史。

## 验证边界

这次执行代码同步、自动测试与正式构建，没有修改游戏规则或界面，也没有更新线上服务器。本次没有重跑浏览器完整对局、GPU 训练或压力测试；训练包原有验证边界以其测试文档为准。
