# 稳定操作区、单行牌河、键盘与同桌复盘测试报告

日期：2026-09-11。实施前计划：`STABLE_CONTROLS_REPLAY_PLAN.md`。

## 实现范围

- 删除操作区左右箭头和“先选一张牌，再次点击或按出牌按钮”说明。鸣牌选项改为手牌下方全宽网格；操作区固定高度，选项变化不推动听牌区或后续内容。
- 上方东、南、西三家牌河单行等比例显示。桌面最近 24 张、窄屏最近 16 张；更多弃牌通过完整牌河弹窗查看，保留顺序、取走状态和摸切标记。
- 公共暗手固定 14 槽，空槽不画牌背；副露另占固定轨道。行动状态一直保留占位，分数独立固定行。自己的摸牌槽不因摸打而销毁。
- A / D 按画面顺序循环选牌，包括独立摸牌；Enter 仅提交选中实体的服务器合法出牌动作；鸣牌阶段空格直接过。输入、弹窗、IME、重复按键、托管、忙碌、过期、无权限和复盘均有守卫；请求失败后可重试。
- 牌谱复用 TableView / PlayerLane；三家历史手牌可见，可切换下方视角。只加载已结束且有访问权的小局；当帧结算使用只读模式，禁用实时确认/退出动作。
- 滚轮向下/向上切换后/前一步；40px 累积、100ms 节流。有效范围内不同时滚动页面，边界放行；表单、弹窗、横向和缩放手势不切步。切步/弹窗暂停自动播放，不替玩家操作正在进行的牌局。

## 自动化验证

`cd frontend; npm test -- --reporter=dot`：**30 个测试文件，705 / 705 通过**。

重点覆盖：键盘独立 hook（49 例）、组件集成、真实身份与合法动作验证、重复提交及失败重试、只读渲染与结算防御、完整牌河弹窗、复盘授权/完成条件/异步竞态/切步边界/自动播放、打开复盘暂停自动摸牌且返回后恢复最新实时状态。

`npm run build`：通过（牌面校验、vue-tsc、Vite 生产构建）。`git diff --check`：通过。

牌面图案没有改动：图集仍为 250050 字节；SHA256 `4e4337f4aa7f9b0ed43134007c10e4cdeb7b42da66ffbd4ff8457af49433ef0f`。验收用预览页面和合成牌谱没有进入生产单入口构建。

## 浏览器与像素验证

使用内置浏览器测试 `stable-table-preview.html`、`replay-layout-preview.html`，以及 `18095` 端口的真实生产构建。浏览器技能依赖未安装，使用现有浏览器工具替代，未安装新软件。

检查视口：320、390、640、900、1024、1280、1440、1920；另测 361 / 641 断点（浏览器实际物理缩放舍入为 362 / 642）。

- 每家 60 张弃牌、四组副露、13 个鸣牌按钮、18 种听牌、超长昵称/提示的压力样例：三家牌河各一行，所有动作按钮在操作区内，没有左右箭头、横向按钮溢出或内部纵向滚动条。
- 1440 宽度对比摸牌前后及行动切换：三个分数、三个副露区、自己的手牌/摸牌槽、操作区和面板坐标/尺寸全部相同。
- 1024 宽度托管切换前后：本人分数、首张手牌、听牌区、提示区、设置区和牌桌下方标尺坐标全部相同。
- 暗牌实测 19 × 26，窄屏等比例缩小。362 / 642 断点四副露未溢出。
- 窄屏手牌及超多听牌保留无可见滚动条的横向触摸/键盘浏览；没有操作区导航按钮。完整牌河/结算弹窗保留必要的正常内容滚动，整页在小屏正常上下滚动。
- 实际按键：D 选最左牌，A 循环到独立摸牌，Enter 提交；鸣牌按钮保有焦点时按 Space 只发送 PASS。
- 实际滚轮：复盘 1→2→1，页面 scrollY 均为 0；频繁滚动受节流保护。切换视角显示“当前视角”，末步显示赢家完整手牌/番型/支付，结算内无确认按钮。弹窗滚轮不改变当前步骤。
- 开发预览、复盘及生产构建检查未发现控制台 error / warning。

## 真实 Java / HTTP 回归

启动独立 Java 服务 `127.0.0.1:18094`，数据仅存 `artifacts/stable-controls-replay-20260911/runtime/rooms.json`，未使用生产数据。

`node scripts/player-lanes-smoke.mjs`：通过。房间 777593，三名独立授权玩家，6 次轮流摸打，18 条命令；越权回合/过期版本被拒绝，手切/摸切正确；自己完整手牌、对手暗牌隐私、108 张实体守恒和总点数 30 均通过。

生产构建浏览器在本地创建房间 914756，添加两机器人开局，实时连接正常。长时间验收时触发服务器正常超时托管；收回控制后，使用 A + Enter 提交刚摸的 `ym-W5-3`，服务器牌河显示“五万 · 摸切”。没有绕过服务器或直接修改房间状态。

## 发布

仅更新前端、相关源文件与文档；不改规则、Java jar、FRP、Nginx 配置或生产牌局数据。

生产入口 SHA256：`cc247d9092e21cc0fa733b6e45b0ee12a5322d154f40372cc6237738e5105cb3`。

已发布到 `http://82.156.207.98:5173/`。发布包 SHA256：`e48d6ad98897cf8252ebd007a4f9705d3a3b71d05b2a68918c69c2c9b2036598`，34 个精确白名单文件。旧入口、源文件及发布包保存在 `/home/leo/mahjong_20260822/backups/stable-controls-replay-20260911`；旧哈希资源保留，入口原子替换。

- 公网入口、JS `index-CLW61hQU.js`、CSS `index-BzwmstvJ.css`、原图集均返回 200，逐个 SHA256 与本地构建完全一致。
- `/api/yaoming/rules`、`/api/yaoming/rooms` 返回 200；规则版本仍为 `26.9 LTS`。发布后可读取原等待房间及进行中的房间，未创建公网测试房间。
- 后端 PID 前后均为 `1521168`，jar SHA256 前后均为 `0f9761d02b5bd45e6f83eeae5afa9a22f6127abc659bf2addfccb881bfe354d4`；后端、nginx、frpc_tencent 持续 active。
- 内置浏览器打开公网首页，显示“已连接”，DOM 加载新版 `index-CLW61hQU.js`，无控制台 error / warning。首次访问的自动化采集超时后复用了原标签页成功确认，没有重复创建或操作公网牌局。
- 临时浏览器验收页已关闭；独立本地测试服务在验收后停止。公网标签页保留供查看。

局限：像素检查覆盖上述尺寸和状态组合，不代表穷尽所有设备/浏览器；JS 单测不提供真实布局，因此几何结果以浏览器实测为准。本轮不修改规则或后端，未重新运行 Java 单元套件，使用原 jar 做隔离 HTTP 与真实浏览器联机回归。
