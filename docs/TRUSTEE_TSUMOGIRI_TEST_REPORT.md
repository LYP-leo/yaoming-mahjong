# 纯摸切托管与最新弃牌栏测试报告

日期：2026-09-09。编码前计划：`TRUSTEE_TSUMOGIRI_PLAN.md`。

## 修复内容与口径

- 红框对应中央最新弃牌条。原44px容器的 `overflow:auto` 会因文字行盒 / 牌面阴影溢出出现滚动条。本次保持44px，使用固定牌列与14px+14px两行文字，外层 `overflow:clip`，长昵称省略并保留完整title/aria；不只是隐藏滚动条，内容本身也完整容纳。中央其他状态格同样不再创建滚动容器；牌河、副露和日志原有内部滚动不变。
- 真人主动、超时、离线托管独立使用 `YmTrustee`，不再调用智能选牌器：自动摸牌、打出刚摸到的实体，响应只过，不吃碰杠或自动和牌。缺少有效摸牌实体时按排序后的最右合法手牌机械兜底，避免吃碰后开托管卡局，该张如实记为手切。取消后重新检查状态，不继续代打。
- 练习机器人及明确离席后的机器人接管仍走 `YmBots` 智能策略，含合法和牌。原准备与局间确认流程保留。界面说明和README已区分两者。

## 自动化与构建

- 前端 `npm test`：18文件，477项全部通过（原466+新11）；素材校验、TypeScript和Vite正式构建全部通过。
- 后端JDK21隔离 `clean package`：18套件，351项全部通过（原311+新40），0失败、错误、跳过。38个构建输入与工作区逐项哈希一致，未使用运行中 `backend/target`。
- 新前端覆盖：44px / 非滚动CSS、长昵称完整可访问名称、被取走状态、横杠不裁剪、真牌河 / 副露滚动保留、三种真人托管说明、取消动作和机器人 / 离席文案区别。
- 新后端覆盖：同牌异实体摸切、可和仍摸切、全部响应只过、不开杠、三种手动杠后补牌摸切、吃碰后最右合法牌兜底、650ms节流、取消托管、离线重连收回控制、三种托管原因重启恢复，以及机器人 / 离席接管在正常和超时路径均保持智能策略。

最终JAR：22,462,553字节，SHA-256 `9c8537ab2bca44ec68ce29243ffb57c697963eb3363df045d0e9cfd70becdaab`。

前端产物：`index-lLQkFJ8e.js`、`index-Ewy0Las_.css`，牌面SVG图集未更改。首页SHA-256：`49e7744c445e4203b80c7e28e86d660cd7fc140b4eceec31642720b74aeec290`。

## 浏览器实测

使用本地隔离 `stable-table-preview.html` 真实组件，开启大量牌河、摸切标记、超长昵称及已取走状态；不连接API，不读取或修改真人身份。

| 视口 | 最新弃牌高度 | clientHeight / scrollHeight | overflow X / Y | 页面横向溢出 |
| --- | --- | --- | --- | --- |
| 1280px | 44px | 43 / 43 | clip / clip | 无 |
| 390×844 | 44px | 43 / 43 | clip / clip | 无（页面375px） |
| 320×700 | 44px | 43 / 43 | clip / clip | 无（页面305px） |

- 长昵称 / 已取走切换前后中央条矩形和牌桌高度不变；桌面牌桌646px / 操作区314px，手机600px / 289px。
- 实测牌底加4px横杠仍在44px框内，至少留有2px余量；截图确认右侧滚动条消失、牌图和横杠完整、昵称单行省略。
- 牌河 `overflow-y:auto` 保留；不会把所有滚动条一并隐藏。临时视口已恢复。

## 真实API专项与发布

隔离 `127.0.0.1:18091` / JDK21实测首次通过，退出码0，用时6206ms：两次托管摸切均核对精确摸牌实体，第二次还观察到自动摸牌中间状态；原13张实体保留。取消后下一回合手动摸牌，观察1912ms后14张及摸牌ID均保持，没有自动弃牌；远低于正常30秒出牌期限。

147次私有视图隐私检查通过；实际动作READY3 / DRAW6 / TRUSTEE2 / DISCARD4。随机片段没有出现响应PASS机会，不将单元测试覆盖误称为该HTTP片段覆盖。正常LEAVE清理测试房间493591后列表0→0、其他房间不变，cleanupErrors为空。核验端口与进程后仅停止隔离PID7016，18091已释放；后台错误及stderr为0。

日志在 `artifacts/trustee-tsumogiri-20260909/smoke-18091/`。脚本只创建自己的三真人测试房间，不声称该短流程完成整局和牌 / 牌谱或机器人智能对战；相关逻辑由回归测试覆盖。

线上联合发布已完成，恢复目录 `/home/leo/mahjong_20260822/backups/trustee-tsumogiri-20260909` 包含原源码、静态资源、停服数据、旧JAR及旧dist。停服前两次检查均无进行中牌局，4个等待 / 结束房间的公开摘要在切换前后完全一致，启动前数据文件哈希未变化。

新后端PID2876802，active/running、NRestarts=0；JAR及线上首页哈希与上述已测试产物一致。现有FRP / Nginx配置未变更。公网浏览器确认在大厅且无输入对话框后刷新，实际加载 `index-lLQkFJ8e.js` 和 `index-Ewy0Las_.css`；正式站点留给用户，不把本地夹具当作正式入口。

发布后经生产Nginx `http://127.0.0.1:5173` 运行同一短验收，退出码0，用时9988ms：

- 两次摸切均精确匹配实体（第二次自动摸牌中间的drawnId也观察到），原13张完整保留。
- 收回托管后的下一回合手动摸牌，14张保持1866ms不被自动打出。
- 105次私有视图隐私检查通过；实际动作READY3 / DRAW6 / TRUSTEE2 / PASS2 / DISCARD4。
- 测试房间240034的三席正常LEAVE并清理；cleanupErrors为空，原4个房间摘要前后完全相同，无其他房间残留或改动。未为测试再次重启服务。

本地8080也已在房间为空时备份并更新，新PID32212，规则 / 房间200、旧接口404、房间仍为空。备份位于本轮artifact目录：`local-runtime-before.tar.gz`、`local-data-stopped.tar.gz`、`local-target-before/`，原数据保留。

编码前备份：`artifacts/trustee-tsumogiri-20260909/source-before.tar.gz`。发布脚本 `scripts/deploy-trustee-tsumogiri.sh` 在两次房间安全检查与备份后切换，保留数据和FRP / Nginx配置；本地更新脚本 `scripts/update-local-trustee-tsumogiri.ps1` 验证精确PID / 路径并保留旧产物。

## 边界

本次范围是中央状态条和托管策略，不改变麻将计番规则及智能机器人选牌逻辑。所列浏览器尺寸和测试路径通过，不代表穷尽全部设备、网络竞争和随机牌序。
