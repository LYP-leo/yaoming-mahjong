# 要命麻将第三批测试报告 · 2026-09-08

> 历史验收记录：下文图集、裁切及“赤”徽章是当时实现，不代表当前方案；即使当时功能测试通过，也不满足用户后续明确的重绘要求。现已由原创 SVG 取代，最新结果见 [矢量重绘测试报告](YAOMING_TILE_VECTOR_TEST_REPORT.md)。结算与自动摸牌测试仍为有效历史记录。

## 范围与结果

按编码前 `YAOMING_OPTIMIZATION_03_PLAN.md` 实现用户提出的三项修正：结算默认展示赢家完整牌面和计番依据；默认自动摸牌及设置开关；严格使用上传图片的 34 张牌面。仅修改前端及验收脚本，后端规则和协议未变。

本地与服务器自动化、生产构建、本地浏览器完整功能验证、公网真实 HTTP 对局均通过。公网首页也已用应用内浏览器打开；该浏览器的进一步设置操作发生工具超时，不计为公网设置流程通过。没有已确认而未修复的本批功能缺陷，但以下测试不代表覆盖所有设备及并发情况。

## 自动化与构建

| 项目 | 命令/范围 | 结果 |
| --- | --- | --- |
| 本地 Java | `mvn test`，17:27 | 364 项，失败/错误/跳过均 0 |
| 本地 Vue | `npm test`，最终 17:45 | 249 项，11 个文件，全部通过 |
| 服务器 Vue | `npm test`，17:41 | 249 项，全部通过 |
| 本地生产包 | `npm run build`，包含最后补充的规则页说明 | 类型检查通过，196 个模块构建成功 |
| 服务器生产包 | `npm run build`，包含最后补充的规则页说明 | 构建成功，资源哈希与本地一致 |

新增/替换的定向测试：

- `autoDraw.test.ts`：40 项。默认开启与本机保存、禁用、只对本人合法 DRAW、托管/机器人/忙碌/断线时禁用、规则/复盘/结算/设置等暂停、身份/房间/版本变化取消旧任务、重复快照不重复请求、跨局下一摸、计时校准、失败不循环、手动竞争、卸载清理、设置焦点及共享快捷偏好。
- `settlement.test.ts`：21 项。自摸/点和、三家视角、完整手牌、和牌实体唯一、碰/明杠/暗杠/加杠、说明直接可见、服务器权威计番与封顶、流局/缺记录/缺结算、原确认逻辑、标题 CSS 隔离。
- `tileArt.test.ts`：60 项，替换原 4 项手绘坐标测试。34 张映射和渲染、坐标越界防护、七条/七筒定位、原图校验和、唯一 SVG 裁切 ID、非法牌兜底、ARIA、红宝牌标识、暗牌不载入正面及正面布局适配。
- 既有私有视图、牌桌布局、SSE、听牌、复盘、恢复身份与旧版游戏测试均在全量中复跑。

## 浏览器实际操作

使用本地 `http://127.0.0.1:5173/`，真实 Java 服务与 Vue 界面，未向浏览器注入模拟手牌或内部游戏状态。

### 结算

1. 三个独立 HTTP 测试身份按合法动作完成真实牌局，随后通过正常恢复码窗口进入测试座位。
2. 房间 `253568`：从放铳者视角查看“验收丙 点和”，默认显示赢家 14 张手牌；真实 6 条和牌实体仅标记一次。旁边可见“门清 1 番”“清一色 3 番”、对应说明、原始总番 4/结算计番 4。
3. 房间 `037828`：真实 7 番点和，赢家 11 张手牌加一组东风暗杠，4 张暗杠牌全部可见。8 筒和牌实体仅一次；番牌 2 + 杠 1 + 门清 1 + 混四连 3，合计 7 番，支付按剩余点数截断的说明保留。
4. 桌面 1280×720：修正后标题为正常块布局；14 张赢家手牌区 clientWidth/scrollWidth 均为 544，无需横滚即可看全。原生牌面、各项依据、点数及底部确认按钮目检正常。
5. 390×844：页面 clientWidth/scrollWidth 均 375；结算详情可独立纵向滚动，长手牌局部横滚；确认区底部 828px，在视口内。320×740：页面宽/滚动宽均 305，确认区底部 724px，在视口内；没有整页横向溢出。
6. 点击“确认结算 · 返回大厅”，等待大厅建房按钮实际出现，正常离席，无失效身份页。测试房间随后由脚本正常清理剩余测试座位。

### 自动摸牌与设置

1. 设置首次打开，“自动摸牌”已勾选，“快捷出牌”未勾选。
2. 关闭自动摸牌、完成设置并刷新页面，再打开设置，仍保持关闭。
3. 浏览器创建专用房间 `554291`，加入两个机器人并准备。自动摸牌关闭时，本人保持 13 张手牌、牌墙 69，按钮为“摸牌”。
4. 在设置窗口勾选自动摸牌后继续观察，仍为 13 张/无独立摸牌，倒计时从 15 秒走到 3 秒；说明设置窗口确实暂停自动操作，但不暂停服务器时间。
5. 关闭设置后，不点击摸牌按钮，自动变为 13 张原手牌 + 1 张独立摸牌，牌墙 68，进入 30 秒待出牌。继续观察约 19 秒仍为 14 张，只出现一次本人摸牌事件，没有重复摸牌或代为出牌。
6. 双击独立摸牌按正常两步操作打出，牌桌进入响应；其他机器人打牌后，本人获得“碰/过”选择，自动摸牌没有替本人响应。随后正常离开测试房间并等待大厅出现。
7. 320×740 设置窗口目检：两个开关、说明和“完成设置”按钮可用；页面宽/滚动宽均 305，完成按钮底部约 618px。测试后恢复默认视口。

### 牌面

- 使用用户最新图片 `codex-clipboard-6896b894-fb14-4af1-9fff-ec63192ad636.png`，原图和仓库资产逐字节相同。PNG 1298×642，319,463 字节，未重绘/重编码/染色。
- SHA-256：`5afb0c03f20d236a84c6e3b420a5c301c00f7e92f9df1e204284f11d139ec439`。服务器最终发布资产亦为该哈希。
- 共享组件按 91×134 原始像素裁切，七筒坐标 `(584,63)`、七条 `(584,277)`；仅移除牌体圆角外的深色背景。
- 在规则页逐行目检新版实际使用的 27 种牌；七筒上绿三斜排/下红四、七条顶红一/下两行左右绿中深，与上传图片一致。浏览器读取对应裁切属性准确；规则页牌面约 43×61px，截图可辨。
- 实际手牌、牌河、结算、暗杠及旧版入口均使用同一个新组件。旧版自动恢复已有座位后仅查看界面并返回新版，没有操作或清理原旧版房间。全部 34 种牌的逐项渲染由自动化测试覆盖，未声称在浏览器手动逐张验收所有旧版牌。
- 原图没有红五：旧版红牌采用明确的“赤”标，不伪造原图红五，不改变游戏计分。资产来源与许可边界见 `frontend/src/assets/mahjong-reference-atlas.md`。

## 真实 HTTP 与实时推送回归

`scripts/yaoming-smoke.mjs` 增加结算断言：赢家完整手牌存在，手牌张数 + 副露组数 × 3 = 14，和牌实体只出现一次，实体 ID 不重复，分项番数相加等于 rawFan，fan = min(8, rawFan)，番型名称/说明完整。

执行 `node scripts/yaoming-smoke.mjs <base> --features`；本地人工结算观察额外使用 `--inspect-final` 暂停清理，然后通过正常恢复窗口查看，不修改牌墙或分数。

| 环境/局数 | 实际和牌与覆盖 | SSE 事件 |
| --- | --- | --- |
| 本地第一场 6 局 | 第 5 局 6 番自摸，第 6 局流局终场；摸/打/吃/碰/三种杠/过/确认 | 979 |
| 本地第二场 5 局 | 第 5 局 4 番点和，归零终场；用于 14 张结算界面验收 | 804 |
| 本地第三场 1 局 | 7 番点和归零，含东风暗杠；用于独立结算确认验收 | 112 |
| 公网 4 局 | 前 3 局流局，第 4 局 5 番点和归零终场 | 605 |

公网动作：DRAW 247、DISCARD 264、PASS 55、ACK 9、CONCEALED_KONG 1、PONG 11、CHI 10、ADDED_KONG 2、WIN 1。完成记录的牌张/点数守恒、终帧与结算一致、当前局隐私、退出后的归档读取均通过。

公网另执行 `yaoming-stream-smoke.mjs`：READY 107ms、实际收到 PING 和 CLOSED；错误身份/缺令牌/已离席座位拒绝验证通过。所有脚本所建房间正常离席，完成牌谱按归档规则保留。

## 测试中发现并处理的事项

- 结算使用语义化 header 后受旧全局 header 的 flex/64px 样式污染，标题挤成一行；已隔离重置并补 CSS 回归。桌面手牌区初版溢出 15px，扩大左列后全部 14 张可见。
- 图集替换尚在编辑时提前跑全量，3 项旧手绘坐标测试失败；随后用原图映射/实体渲染测试替换，全量通过。
- 生产类型检查发现项目未声明 Vite 图片导入；新增 `src/vite-env.d.ts` 引用 Vite 客户端类型，重新构建通过。
- 首次结算确认后立即让脚本同时退出同一个测试座位，出现失效身份页。按两个客户端不同 requestId 竞争排查；另建独立测试局，先等待浏览器真正回到大厅再清理，正常通过。未为测试清理竞争修改业务逻辑。
- 应用内浏览器首次打开公网返回 ERR_BLOCKED_BY_CLIENT；重试后成功载入正式首页，能看到新“设置”入口。之后进一步点击设置/读取界面遇到浏览器工具 60 秒超时，故公网设置 UI 端到端不计通过。完整设置与自动摸牌已在本地真实浏览器通过，发布资源哈希与本地相同。
- `browser-act` 技能环境检查发现 CLI 与 uv 均不可用；说明后沿用现有应用内浏览器，未额外安装软件。

## 发布与数据保护

- 前端源代码、类型声明、图集和最终正式构建同步至 `dell:/home/leo/mahjong_20260822`。
- 更新前备份：`/home/leo/mahjong_20260822/backups/yaoming-opt03-20260908/before-opt03.tar.gz`，包含旧前端源码/构建、部署配置、文档及脚本。旧哈希静态资源未清除。
- Vue 正式目录 `/var/www/mahjong-yaoming-20260907`；最终主脚本 `index-TaKb3rZ0.js`，样式 `index-CKibX3XO.css`，图集 `mahjong-reference-atlas-DIAdWhHt.png`。公网首页 HTTP 200，返回新资源。
- 发布只更新前端，没有重启后端、修改 FRP/Nginx 配置、增加公开端口或改计番规则。`mahjong-backend` 与 `nginx` 均 active。
- 既有房间 `864106`（MATCH_END、2 人）和 `016643`（MATCH_END、3 人）发布后保持原样；没有操作或删除用户数据。
- 后端仍为上一批已部署 JAR，SHA-256 `6d5757cad8dc3da07093dab50ce7993d6227e0b5de01c31af40464072aae6c9b`。本批在本地复跑 Java 全量，未重新打包/发布 Java。

## 边界

自动摸牌只是当前设备的操作辅助，不是服务器托管；关闭页面、查看其他页面或网络失败时仍遵循原超时规则。手机验收为模拟视口，未覆盖所有实体手机/网络。结算依据使用服务器原有番型明细，本批未实现额外的面子拆解动画、计番算法重写或牌谱导入编辑。
