# 四人实验版3番起和：测试与发布报告

日期：2026-09-12

## 实施范围

四人实验版起和门槛4→3；三人版仍4，均8番封顶。番种自身番数、排除关系、牌组及支付倍率未变。计划见 `FOUR_PLAYER_THREE_FAN_PLAN.md`。

后端通过 `YmRules.minimumFan()` 统一和牌资格与提示，前端按规则ID显示对应门槛。四人全不靠2番加不求人/杠上炮1番可和，普通2番点和仍拒绝；机器人重新考虑全不靠路线及3番合法等待，鸣牌策略门槛按房间规则调整。

旧四人房间恢复时通过服务端 `appliedMinimumFan` 标记一次性更新房间版本，使客户端刷新听牌、旧版本写入失效；二次恢复不重复递增，三人旧房间不受影响。不延长原响应期限、不重开已提交的过牌、不重新计算已完成结算或历史牌谱。

## 实际验证结果

| 检查 | 结果 |
| --- | --- |
| 后端全量测试 | 30个测试类，565项通过，0失败/错误/跳过 |
| 后端构建 | `mvn -q package`通过；新增集成测试加入后再次`mvn -q test`全量通过，业务源码未改变 |
| 前端全量测试 | 35个测试文件，833项通过 |
| 前端生产构建 | TypeScript、素材校验、Vite构建通过 |
| 隔离HTTP全流程 | 13个场景全部通过，固定127.0.0.1:18094，未创建生产测试牌局 |
| 浏览器生产构建 | 四人牌桌/规则手册显示3番，切三人显示4番，三番点和结算分项/支付正确 |
| 差异检查 | `git diff --check`通过 |

新增/更新覆盖包括：普通3番自摸、3番点和与公开副露、2番非法WIN不改版本；全不靠720种结构及所有单张缺失等待、全不靠自摸3番与实际暗杠→补牌→弃牌→杠上炮3番；不额外叠加门清。听牌仅用自身及公开牌，3番自摸/2番点和分别标记；机器人保留全不靠及普通3番等待。三人3番仍不和、4番和，双规则缓存隔离，8番封顶和原有回归全部通过。

HTTP场景覆盖四人5番/4番旧正例、全不靠自摸3及普通点和2拒绝、平和自摸3、普通点和3、四人自摸2拒绝、三人自摸3拒绝/4通过、北到东吃牌、南四流局终场，以及三人/四人正常建房入座准备各打两圈。每步验证136/108张实体、40/30点守恒、暗牌权限、结算/牌谱一致。

浏览器本地生产构建预览中，“门清1番+混一色2番”点和显示总3番，赢家14张完整手牌与唯一和牌标记，四家分数20/10/10/0，应付12点而余额10点只支付10点。规则手册四人3番、三人4番，四人桌边及服务器提示均为3番。

## 构建标识

- JAR SHA256：`0a35f3b5ad64ec929fec08f421dfaf03e5ffb6dd4794bb3e49c01777acf43bac`
- 首页 SHA256：`885002b07c46610823e660b3b667395e797c092556c08d8c6f7cbca5869fdbaa`
- JS：`index-B9ztNxF4.js`
- CSS：`index-3GgWz74l.css`
- 牌面素材未变。

## 发布

已完成前后端同步发布。发布前和停入口后两次检查：6个房间均为等待或终场，没有进行中对局。备份保留在服务器 `/home/leo/mahjong_20260822/backups/four-player-three-fan-20260912`，未删除此前备份。恢复后6个房间及所有历史结算/牌谱核对一致。

后端、nginx、frpc_tencent均active；后端PID2989054，NRestarts=0。未调整网络或FRP配置。公网首页、JS、CSS及牌面资源均HTTP200且下载哈希与本地测试构建一致。公网两个规则接口分别返回“三人108张 · 4番起和”和“四人136张 · 3番起和”，均8番封顶。

本机缺少BrowserAct及uv，未安装新依赖，改用现有浏览器工具完成本地生产构建交互验收。公网浏览器自动化刷新仍超时，本次不声称公网浏览器交互通过；公网核验依据实际HTTP资源、规则接口及服务器服务状态。
