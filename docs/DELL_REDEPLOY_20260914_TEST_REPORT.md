# Dell 重装恢复部署测试报告

日期：2026-09-14。计划见 `DELL_REDEPLOY_20260914_PLAN.md`。

## 部署结果

已在 SSH `dell`（192.168.3.101，Ubuntu 26.04.1，leo 用户）重新部署现有游戏，没有修改游戏规则或前端功能，没有部署训练 AI。

- 源码与后端：`/home/leo/mahjong_20260822`。
- JAR：`backend/target/mahjong-server-0.1.0.jar`，22,480,814 字节。
- Vue 正式构建：`/var/www/mahjong-yaoming-20260907`，7 文件，568,471 字节。
- Java：服务器通过 Ubuntu 软件仓库安装 OpenJDK 21.0.12 headless；Nginx 1.28.3。另安装 curl 用于诊断，没有升级/删除既有软件包，没有在服务器安装 Node/Maven。
- `mahjong-backend`、`nginx`、`frpc_tencent` 均 active 且 enabled。
- 后端只监听回环地址 8080；Nginx 监听本机 5173 和 80。公网页面入口为 `http://82.156.207.98:5173/` 及 `http://82.156.207.98/`。
- FRP 配置前后哈希一致，进程 PID 1912 未变化；没有重启 FRP、改认证信息或影响其它隧道。
- 原项目/静态路径不存在，在检查范围内未找到原 `yaoming-rooms.json`。从空数据开始，没有导入本地测试历史。其它旧麻将项目及 SQL 文件均未改动。
- 首次部署恢复目录：`/root/mahjong-redeploy-backup-20260914`，保存 Nginx 默认配置、原默认站点启用链接、验证记录。默认站点链接移入该目录，可恢复，未删除用户站点。

## 构建与回归

| 项目 | 实测结果 |
|---|---|
| 前端全量测试 | 37 文件，902 项通过；Vitest 29.46 秒 |
| Vue 正式构建 | 牌面源文件完整性校验、TypeScript、Vite 构建均通过，14.99 秒 |
| 后端全量测试/打包 | Maven 3.9.9 + JDK 21.0.10；31 个类，588 项通过，0 失败/错误/跳过；33.092 秒 |
| Java 编译目标 | class major 65，即 Java 21 |
| 部署脚本 | `bash -n` 通过，246 项归档安全检查通过，部署退出码 0 |
| Nginx | `nginx -t` 通过 |
| 公网首页 | 5173 和 80 均 HTTP 200，首页对应本次构建 |
| 公网主 JS/CSS/牌面图集 | 均 200，大小分别 228546 / 81553 / 250050 字节 |
| 退役/私有路径 | `/ws/info` 返回 404，`/.env` 返回 403（安全拒绝，不是创建房间错误） |
| 重启恢复 | 确认只有本次测试房后正常重启后端，2 个测试房状态恢复，公网再次 200，NRestarts 为 0 |
| 数据权限 | backend/data 为 leo:leo 700，yaoming-rooms.json 为 leo:leo 600 |
| 补丁检查 | `git diff --check` 通过 |

启动等待期间 curl 出现短暂连接拒绝属于服务尚在初始化，随后约 2 秒内启动成功；服务日志未出现部署错误。后端测试中刻意触发的持久化异常属于失败回滚用例，测试均通过。

## 公网联机 API 实测

脚本 `scripts/dell-redeploy-smoke.mjs --run` 显式固定公网目标，仅创建/操作本次测试身份。报告 `artifacts/dell-redeploy-20260914/public-smoke.json` 不包含恢复令牌、手牌或原始事件。

- 三人规则：2 真人 + 1 机器人，房间 `732045`；8 次真人游戏动作，4 次机器人动作，约 7.4 秒。
- 四人规则：2 真人 + 2 机器人，房间 `123034`；8 次真人游戏动作，6 次机器人动作，约 10.3 秒。
- 两组均通过：同源 Origin 创建（201）/加入/准备，无 403；身份恢复成功；错误身份 400；旧版本操作 409 且未改状态；所有他人暗手隐藏，双方均可读取自己手牌。
- SSE 实际收到 READY 和 CHANGED 版本通知（三人 15 次 CHANGED，四人 18 次），不是只检查接口状态码。
- 摸牌、打牌、过牌及机器人行动推进成功；双方真人身份在 finally 中均成功 LEAVE。
- 测试房剩余机器人不算真人，将按既有 10 分钟空房保留期自然回收；离场时间分别 13:40:31 和 13:40:41 CST。没有直接删除任何房间或修改生产保留期，未等待并实测本次的到期回收。

## 浏览器验证与边界

通过内置浏览器读到公网大厅完整可访问性树：标题“要命麻将 · 三人及四人对局”、已连接、创建牌局/输入房间号/规则手册/设置等入口正常出现。随后浏览器工具在刷新和新建测试页时多次超时；**未完成本次浏览器自动点击、截图和布局回归**。不将 API 测试冒充完整 GUI 测试。

原有 BrowserAct CLI 及 uv 在本机均不可用，未安装新浏览器工具。生产上只完成短局动作验证，不声称此次公网已打完全部三/四人整场；相关引擎和界面逻辑由全量自动测试覆盖。没有进行压力测试、真实操作系统重启、TLS 配置或历史数据恢复。

## 交付校验

- 部署归档 `application.tar.gz`：20,750,823 字节，SHA256 `01d90c12c50eb990a29db88a75ad407747d7399d0e4155f325c9c56bcb2be56e`。
- JAR SHA256：`e33af1dab80fe1f491c8b93c7b8464410022dbd710c79c34ea88b64e7243ae24`。
- index.html SHA256：`c3350e9e9d4993d76c600f5ac99348909ae48a1cae5d0c69bf3178e9528c4643`。
- 首次部署脚本 SHA256：`9d8da9b2cb711e2ca5ab05369ac18ccfcc35df10c213bb7594cd86ff31976185`。

归档包含游戏源码、依赖锁文件、牌面原始矢量文件及许可、已验证构建与必要配置。明确排除了本机 backend/data、训练包、SSH/FRP 凭据、node_modules、.git 和其它历史项目。测试报告和本次部署/烟测脚本在部署完成后另行同步，未重打包上述归档或修改已测试构建。

## 部署后牌面显示专项检查（2026-09-14）

用户反馈牌面不显示后进行只读检查，没有修改游戏代码、替换素材或重启服务。

- 公网图集 `/assets/mahjong-graphic-atlas-C-odaEvj.svg` 返回 HTTP 200，Content-Type 为 `image/svg+xml`，大小为 250,050 字节（约 244 KiB）。
- 服务器图集与本地源文件/构建文件 SHA256 一致：`4e4337f4aa7f9b0ed43134007c10e4cdeb7b42da66ffbd4ff8457af49433ef0f`。Nginx 访问日志中有浏览器成功下载该图集的记录，未发现图集 404。
- 图集 XML 解析通过，37 个牌面格子及坐标映射完整，未发现缺失颜色定义或重复图形 ID。
- 本次内置浏览器实际打开公网 5173 首页并取得截图：一条、五万、九筒正常显示。
- 点击“规则手册”并取得完整页面截图：三人规则的 27 种牌面均正常显示，包括七条、七筒及字牌；白板为空白牌面属于原素材设计。
- 当前未复现用户端的空白牌面，不能据此断言是缓存问题，也不声称已修复未定位的问题。本次没有创建或操作用户对局；若用户仍能复现，需要具体页面截图和浏览器信息继续定位。

以上补充了本报告早先因浏览器超时而未完成的首页/规则手册视觉验证，不等同于完整对局 GUI 回归。
