# 和牌提示按场景显示：测试记录（2026-09-12）

## 本次结果

- 托管时不显示和牌提示或遗留自动摸牌通知，保留托管状态、“收回控制”按钮。
- 三人/四人常驻规则摘要隐藏；服务器没有提供提示时保持空白，不再填充通用规则兜底。
- 当前手牌判断、实际可自摸/点和提示、番数不足与吃碰后限制保留；普通用户自动摸牌失败的操作提示仍保留。
- 听牌牌面、番数、剩余张数及增量更新逻辑未改。所有固定高度样式未改。
- 仅精确匹配已知完整规则摘要（兼容空白、中文/ASCII 标点及句末句号），不因实际判断文字包含“起和”“封顶”等关键词而误删。

## 自动化

- 新增 `contextual-win-hint.test.ts`：48 项，覆盖两种规则、手动/超时/离线/未标注原因托管、空提示、摘要过滤、保留实际判断、对手托管、只读复盘、收回控制及固定容器/听牌 DOM 连续性。
- 更新规则与布局测试中的旧常驻兜底断言；没有降低固定轨道和规则番值的断言。
- 针对性测试：6 个文件、139 项通过。
- 全量前端：37 个文件、902 项通过。
- `npm.cmd run build`：牌面素材校验、vue-tsc 类型检查、Vite 生产构建通过。
- `git diff --check`：通过。后端业务未修改，本次不重复运行后端测试。

## 真实浏览器

使用本地隔离的 `stable-table-preview.html` 合成场景，不访问 API、不创建真实房间。browser-act/uv 未安装，使用现有内置浏览器完成检查，没有安装额外依赖。

1366×768 与 390×844 两种视口，三人和四人均验证：

1. 摸牌后分别显示“尚未成和牌形，至少需要4番/3番”。
2. 开启托管后和牌文字清空；托管中打出摸牌后仍为空；取消托管且未摸牌时，常驻规则摘要不出现。
3. 未摸牌的静默状态仍可展示 18 张合成听牌，重新摸牌后判断恢复。
4. 详细判断与自动摸牌通知在普通状态显示，进入托管后两段文字均隐藏。
5. 每次切换前后比较牌桌、手牌面板、手牌行、动作区、听牌区、提示容器的 x/y/宽/高，全部完全一致；没有页面横向溢出。
6. 浏览器错误日志为空；验收后关闭本地标签页，保留用户原有线上页面，不代用户刷新或操作对局。

## 发布

已发布到 `http://82.156.207.98:5173/`。公网入口、JS、CSS、牌面图集 HTTP 200，内容哈希与本地构建完全一致；规则和房间列表接口 HTTP 200。

- 入口 SHA256：`c3350e9e9d4993d76c600f5ac99348909ae48a1cae5d0c69bf3178e9528c4643`。
- JS：`index-B92QgohM.js`，228,546 字节；SHA256 `76b81aaa33d2421dcb783df598e477c2122655254d07bc4ae8316edb7a29146d`。
- CSS、牌面图集均未变化，包含上一版四人 614px 牌桌布局。
- 发布前后后端 PID 均为 `3012626`，JAR 哈希不变，后端/nginx/FRP 始终 active；发布后存在正在进行的对局，本次没有向房间发送写入请求。
- 备份：`/home/leo/mahjong_20260822/backups/contextual-win-hint-20260912`。
- 交互/几何验证使用本地隔离浏览器；公网验证是 HTTP 和静态资源哈希检查，不是创建真实房间的线上端到端对局测试。

前端发布保留旧哈希资源，最后原子替换入口，不修改房间数据、不重启服务。
