# 取消错误振听限制：测试报告

日期：2026-09-09。对应编码前计划：NO_FURITEN_PLAN.md。

## 确认规则与实现

用户确认文档没有舍牌振听或过和振听。本次取消两类限制，而非只改显示。引擎不以历史舍牌/过和集合拒绝点和，也不再写入临时禁和记录；听牌候选、假设弃牌后等待、智能机器人的有效点和进张和风险判断均同步修正。规则 API、默认提示、规则手册及演示样例不再传播旧限制。结构、4 番门槛、截和优先级、计分和纯摸切托管保持原有逻辑。

历史存档字段保留用于兼容，不删除房间或牌谱数据。过去文档的旧限制描述仅作历史记录，不代表现行规则。

## 自动化验证

- 前端：19 个文件、483 项测试通过；牌面校验、TypeScript 检查、正式构建通过。新增 6 项无振听回归；CSS 和牌面素材未改变。
- 后端定向：91 项通过，包括实际重复点和、超时过、主动过、旧数组及对象存档、0 张计数与隐私。
- 后端首次全量：361 项中 1 项新增机器人假设弃牌夹具的期望未通过。核对发现夹具漏算了打九条后的三条进张，生产策略选九条是正确的。补充公开三条后，测试明确枚举两候选均为 2 张、5 番；恢复正确源码后 21 项机器人测试通过。只在隔离副本重加旧假设弃牌禁和条件，测试如预期失败，再恢复并 clean test 通过，证明回归能检出旧问题。没有跳过失败测试或为迁就测试更改策略。
- 二次审查发现旧 REACTION 快照沿用旧选项导致缺 WIN 的边界，已列入计划补充和恢复兼容补丁；验证未响应座位、已提交 PASS、原截止时间及过期窗口。
- 最终后端：JDK 21 隔离 `clean package`，20 个套件、377 项全通过，0 失败/错误/跳过。39 个输入源码/测试/资源文件逐个哈希与主工作区一致。最终日志 `backend-final-build.log`。
- 恢复边界新增 16 项全通过：对象/数组存档、已存在/缺 PASS/整座位遗漏、原时限保留、重复恢复幂等、已提交 PASS/PONG 保留、过期及 3 番不授予 WIN。
- 实际 HTTP：启动专用本机 18092 服务，带非空历史字段和自己已打出一条的合法 108 张存档。通过普通 REST 执行“甲打一条→丙过→乙摸牌并打一条→丙点和”，丙期间没有摸牌，正常 4 番结算。六次私有视图检查、三家结果一致、赢家 14 张完整手牌、总点数 30；公开余张依次 3/2/1，全部通过。日志 `http-smoke.log`。临时服务已停止，隔离快照保留作证据，没有操作线上房间数据。
- 本地 8080：确认房间为空后备份更新，PID 37072，规则与房间接口 200，旧版接口 404，数据保留且房间仍为空。日志 `local-update.log`。
- 浏览器：实际打开本地 5173 页面并点击“规则手册”，确认静态说明和真实后端规则均明确“没有舍牌振听或过和振听”“无需等自己摸牌”，同时保留合法牌型与 4 番门槛。当前控制台 error 列表为空。未改用户线上浏览器的牌局状态。

## 构建与发布

- 后端 JAR：`64164f6b4ea83cdcd13494ed07cc80b4a89435ca0b0aa7158d8fc95e98c38117`。
- 前端 index：`25ac667c78d8066ce34d38af3335a9d71e962c3d1069ec57392693a751ce7e51`，JS `index-2_uTAn_Z.js`。
- CSS `index-Ewy0Las_.css`、上游 SVG 图集均与上一版哈希一致；没有改布局和牌面。
- 上轮暂缓记录：14:40 与 14:41 两次发布尝试均被活动牌局检查拒绝，未执行停止、替换或重启。两次之间曾看到终场状态，但再次发布时已有新对局，因此当时没有继续发布。后续用户明确授权直接中断对局重启，执行结果见下节。
- 上轮未发布时服务保持原 PID 2876802，active/running，NRestarts=0；旧 JAR 为 `9c8537ab2bca44ec68ce29243ffb57c697963eb3363df045d0e9cfd70becdaab`，旧 index 为 `49e7744c445e4203b80c7e28e86d660cd7fc140b4eceec31642720b74aeec290`，保留为回退版本记录。
- 已上传待发布包至服务器 `/tmp/mahjong-no-furiten-20260909.tar.gz`（SHA256 `b2e999472b5d8b03d05787db2dda5e4b8c94ff1e582faf03a08b46b6c6db0cbc`）、同前缀 `.jar`、`/tmp/deploy-no-furiten-20260909.sh`；脚本检查在备份/替换前退出。下次发布仍必须重验哈希、房间状态和运行版本。最终报告另行同步，不修改已验证发布包。

## 证据与恢复

- 本地证据目录：`artifacts/no-furiten-20260909/`。原源码备份 `source-before.tar.gz`；首次全量日志 `backend-build.log`。
- 隔离实际 HTTP 脚本：`scripts/yaoming-no-furiten-smoke.mjs`。只允许专用本机 18092 端口，不接受公网；固定独立牌局数据，所有 108 张实体唯一且守恒。通过正常 REST 动作验证“先过，再次点和”，不增加线上测试接口。
- 发布脚本：`scripts/deploy-no-furiten.sh` 与 `scripts/update-local-no-furiten.ps1`，校验旧版本与新构建哈希，检查活动牌局，备份并保留数据，失败恢复原版本。
- 发布脚本复核加固：停止服务前先安装错误恢复处理；校验整个源包 SHA256，解包前拒绝绝对/父级路径、链接和特殊条目，仅接受源码/静态资源/文档/脚本白名单，禁止包含运行数据或 target。

## 用户授权中断对局后的发布与线上验证

2026-09-09 约 14:46 完成发布，14:53 完成后续验证。用户明确允许打断所有对局直接重启，使用显式 `--allow-active-games` 参数；默认脚本依旧保护活动对局。

- 独立复核确认游戏源码及已测试的 JAR/dist 未变化，无需重复构建。仅重新打包更新后的计划、报告和一次性授权发布脚本，源包 `release-authorized.tar.gz` SHA256 为 `28a8d47c109e6c692d1d3a33e446cafba3301f0c8f594fbaca2576abe4e64586`。
- 发布前两次检查均为 5 个房间、1 个进行中；按授权停止后端，先备份停止状态的完整数据，再安装通过 860 项前后端测试的产物。部署过程校验数据文件在启动前未改变，未清空房间、牌谱或玩家身份，也未更改 FRP/Nginx 配置。
- 发布前后均保留同一组 5 个房间，原进行中房间 390604 从 NEED_DRAW 正常推进到 REACTION。活动模式不强求阶段不变，旧倒计时及调度按既有规则继续。
- 新服务 PID 2916008，active/running，NRestarts=0；服务器 JAR 与 index SHA256 分别与本报告列出的 64164f…38117、25ac66…e7e51 完全一致。Nginx 和 frpc_tencent 均 active；新版规则/房间可访问，旧 `/api/rooms`、`/api/rules` 均 404。
- 公网实际测试：新建自有测试房 243096，三真人身份准备开局、自动摸牌、连续两次精确实体摸切、原 13 张保留、取消托管后保持控制、81 次私有视图检查均通过，约 12.4 秒完成，测试房已清理且无清理错误。脚本最终退出 1 是其“其他房间摘要完全不变”断言：原房间 390604 在此期间从 REACTION 正常推进为 NEED_DRAW；ID、人数和其余房间均不变。没有篡改或恢复其他人的房间，不能把该脚本整体记录成零退出通过。详情 `artifacts/no-furiten-20260909/public-smoke.log`。
- 浏览器实际刷新公网已有页面，加载 `/assets/index-2_uTAn_Z.js`，显示“已连接”；点击“规则手册”，静态说明与服务端说明均确认自己打过/曾放过的同种牌仍可点和，无需等自己摸牌，保留合法结构与 4 番门槛。无页面告警。
- 公网首次 PowerShell 首页请求有一次 15 秒超时；后续浏览器成功刷新，并以独立 HTTP 检查确认首页 200、约 0.094 秒，服务器本机首页 200、约 0.0003 秒。规则 API 与实际游戏请求成功，未观察到 502/403。
- 可恢复备份：服务器 `/home/leo/mahjong_20260822/backups/no-furiten-20260909/`，含 `source-before.tar.gz`（约 487 KB）、`public-before.tar.gz`（约 291 KB）、`stopped-data-before.tar.gz`（约 866 KB）、原 target/dist；备份为私有权限。所有本轮提权 SSH 会话已退出。

当前状态：本地与线上均已更新；取消舍牌振听、过和振听已在线上生效。部署及测试记录完成。
