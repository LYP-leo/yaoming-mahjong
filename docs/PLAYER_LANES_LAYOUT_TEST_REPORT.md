# 玩家纵向分区测试报告

日期：2026-09-11。对应计划：PLAYER_LANES_LAYOUT_PLAN.md。

## 实现结果

- 正式牌桌固定东、南、西三行；依据服务端 wind 排序，不按永久 seat 或轮次行动者排序。
- 本人始终以金框标记；三家均以 handSize 渲染牌背，包含本人。真实牌面只在底部本人手牌区呈现，副露与牌河各自归属对应玩家。
- 上游 mahjong_graphic 图集未修改。桌面牌背实测 19×26，小屏 14×19.15（比例舍入误差小于 0.02px）。
- 听牌唯一挂载在底部手牌区域，保留自动计算、剩余张数及有效番数，不增加空提示文字；高度固定 96px。
- 每家展示最近最多 24 张弃牌，小屏为 16 张；查看全部打开真实完整牌河，保留手切/摸切、原始顺序、最新牌标记与取走状态。
- 局况/最新牌/日志/规则/复盘/恢复码/开门记录归侧栏。长昵称和近期日志固定空间展示，完整记录可主动展开。
- 窄屏不使用遮挡页面的固定手牌底栏，允许正常整页纵向滚动；牌桌内部无狭小竖向滚动条。超长手牌/听牌可横向浏览，多动作有前后导航按钮。

## 自动测试

`npm.cmd test -- --reporter=default --reporter=json --outputFile=D:/mahjong/artifacts/player-lanes-implementation-20260911/frontend-tests.json`

- 27 个测试文件，616 / 616 通过。
- 新覆盖：本人牌背、三个视角与换庄重排、本人标记与行动者分离、暗手/暗杠隐私、24/16张河、完整河焦点/键盘/遮罩关闭、信息栏及恢复码、应用级听牌唯一挂载、牌河查看时自动摸牌暂停与恢复。
- 原动作确认/快捷出牌/自动摸牌/听牌稳定更新/规则与结算/牌谱/断线与实时推送测试继续通过。
- `npm.cmd run build` 通过：图集完整性校验、Vue/TypeScript 检查、Vite 生产构建。
- `git diff --check` 通过。

## 隔离真实 API 回归

脚本：`scripts/player-lanes-smoke.mjs`。仅允许 http://127.0.0.1:18094；数据文件新建于 artifacts/player-lanes-implementation-20260911/runtime/rooms.json，未接触生产数据。

- 3 真人创建/加入/准备，6 轮实际 DRAW/DISCARD（3 摸切、3 手切），共 19 条命令通过。
- 所有视角：本人手牌完整、旁家 hand 为空、handSize 正确、风位正确。
- 非法回合返回 400、旧版本返回 409；每步验证 108 张守恒、实体不重复、总计 30 分不变。
- 测试房间 778319，牌山 69→63。本轮没有修改 Java 后端代码，未重复执行上一规则阶段的 Maven 单元测试。

## 浏览器验收

- 本地隔离布局页检查宽度 320、390、920、1024、1280、1440px，涵盖每家60张河、四组副露、长消息、18种听牌、多杠选项。
- 页面没有水平溢出；检查 .ym-play-layout 内所有 overflow-y:auto/scroll 元素，超高滚动元素为 0。弹窗完整内容允许正常滚动。
- 390px：三行高度均164px；牌背14×19.15；修复后所有公共副露区域 scrollWidth=clientWidth。320px同样无溢出。
- 桌面行高158px、手牌区275px，空/满提示及牌河变化不改变轨道高度。恢复码、完整日志等由用户主动展开，不自动反复展开/收起。
- 完整牌河查看到60张，关闭按钮/遮罩/Esc可退出；普通房间更新不会误关窗口。
- 最终生产构建通过本地18095反向代理连接隔离18094后端：创建房间809394、添加2机器人、准备、13→14自动摸牌、选牌不立即提交、确认摸切、牌河横线均实测正常；刷新恢复原座位，换庄后东南西顺序与西位本人金框正确；浏览器 error/warn 为0。
- 多动作前后导航可用，杠牌第12个选项与PASS精确提交展示动作，不执行任何伪造牌局推进。

## 本轮发现并修复

1. 新版CSS初版移动端牌背间距calc单位不匹配、双行河轨道不足；修正为合法比例及固定空间。
2. 副露与过多暗牌压力场景挤压；小屏分两层放置公开手牌，保持比例并防越界。
3. 新增牌河弹窗/恢复码监听数组导致每次房间更新误关闭；使用稳定多source监听，并增加回归。
4. 机器人同时带trustee标记时误显示人类“自动摸切”；机器人与人类托管文案区分。
5. 901–940px侧栏断点不一致；统一900px，避免窄栏分两列。

## 发布

已执行 scripts/deploy-player-lanes.sh，仅前端发布成功；原子替换入口，旧哈希资源保留，未修改规则、FRP或Nginx配置。

- 后端发布前后PID均1521168，NRestarts=0；JAR SHA256仍为0f9761d02b5bd45e6f83eeae5afa9a22f6127abc659bf2addfccb881bfe354d4。
- 发布脚本采集的前后房间摘要相同（016643、262741、107661）；没有停止服务、清除或迁移牌局数据。
- 回滚备份：/home/leo/mahjong_20260822/backups/player-lanes-20260911。
- 发布包 SHA256：c740e4738df1a1b5d1c307b812a04444c0608b07bcd2031158df272a8ae27979。
- 公网 http://82.156.207.98:5173/ 的首页、JS、CSS、SVG、规则和房间列表均返回200。首页和三个资产逐字节哈希与本地构建相同。
- 首页 SHA256：fd0fe443b506f8ae762346485749e6b834cfa0b07393c44c9f8a57fa8dc752e6。
- JS index-BRt-83RP.js：49c217a553abae484553aa65a885f086678525f6264e6a26184b8b672b0f9735。
- CSS index-DH9u_reI.css：cd7c72411d843a2505da4dd1760fa73bcdec66cbd24cf43088e8ebb7b53b7dac。
- 公网浏览器实际打开首页成功，显示“已连接”；未在生产环境创建测试对局。
- 核对脚本 artifacts/player-lanes-implementation-20260911/verify-release.mjs 首次本地路径写多一级导致ENOENT（尚未发出公网请求），修正相对路径后全项通过；并非网站故障。
