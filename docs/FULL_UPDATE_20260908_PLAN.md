# 前后端联合更新计划 · 2026-09-08

用户明确要求停止后端，前后端一起更新。沿用 dell 上现有 Java + Vue + Nginx/FRP 部署，不迁移托管方式，不更改规则或清空房间。

## 顺序与保护

1. 核对实际 systemd 服务与 JAR 路径，停止 `mahjong-backend`，确认端口 8080 不再监听。
2. 停服后备份现有源码、JAR、前端产物、部署配置和完整 `backend/data`。备份只保存在服务器，限制读取权限；不以本地测试数据覆盖线上数据。
3. 比较本地/服务器源码；仅同步确认的程序文件。前后端均重新测试和构建，后端使用服务器完整 JDK 25，以 Java 21 为编译目标。
4. 停服日志显示 Spring 已完成优雅关闭，但 systemd 将 Java 的退出码 143 标为失败。给服务添加 `SuccessExitStatus=143`，保留用户、工作目录、端口、内存限制和原重启策略。
5. 先发布前端新哈希资源，再替换入口；安装上述服务配置并重新加载 systemd，启动重新构建的后端。
6. 检查新进程、源站/公网首页与只读 API、既有房间恢复情况及日志。若构建失败，不发布失败产物；若启动失败，保留诊断并回退备份程序，不覆盖用户数据。
7. 编写联合更新测试报告，记录实际测试数量、发布版本和恢复结果。

## 当前停服证据

- 2026-09-08 20:53:48 CST 后端完成优雅关闭，MainPID=0，8080 无监听。
- 备份目录：`/home/leo/mahjong_20260822/backups/full-update-20260908-2100`。
- 停服后用户要命麻将数据 SHA-256：`b18840cdf7dd4844854a610227ca3c0984f8604bf765fe5274119233feb4eea8`。只记录校验和，不输出恢复凭据或牌墙内容。
