# 摸牌默认选中与空格出牌测试报告

日期：2026-09-11。实施前计划：`DRAW_SELECTION_SPACE_PLAN.md`。

## 变更

- TableView 收到本人可出牌回合的合法 `drawnTileId` 时默认选中该实体；普通摸牌、杠后补牌与初次挂载均适用。只选中、不提交；无摸牌或无合法动作时留空。
- 玩家鼠标/A/D 改选后，普通版本、消息、在线状态刷新不覆盖选择。阶段、手牌、权限等有效上下文变化时重置。
- Space 和 Enter 在出牌阶段确认当前选中的合法牌，共享防重复提交锁；Space 在响应阶段仍只发送 PASS。原生按钮点击/页面滚动不能造成额外提交或误杠。
- 保留输入、IME、弹窗、只读、忙碌、托管、超时以及失败重试保护。快捷出牌开启时默认选择也不会自动打出。

## 测试

- 新默认摸牌组件集成：31 / 31 通过。覆盖补牌同图案不同实体、无实体/不合法/多ID动作、无摸牌、他人回合、只读、托管、过期、心跳保留手动选择、Space/Enter共享锁，以及焦点与选中实体不同时仍打正确牌。
- 键盘 hook：68 / 68 通过。覆盖普通Space出牌、REACTION始终PASS、无选牌与各类禁用状态、焦点手牌/杠按钮、两键交替/长按防重复、同版本失败后重试。
- 全量前端：32 文件，**760 / 760 通过**。
- `npm run build` 通过：牌面校验、TypeScript、Vite；`git diff --check` 通过。未改变 CSS、图集、后端或规则。
- 内置浏览器对真实 TableView 操作：初挂载独立摸牌 `own-hand-0-13` 已选中，动作记录仍为空；Space 后只出现 DISCARD；去除摸牌后选中数量为0；重新摸牌后 A 改选 `own-hand-0-5`，消息刷新后仍选它；重载后 Enter 可直接确认默认摸牌；进入鸣牌后，即使焦点在杠按钮上 Space 也只出现 PASS。控制台无 error / warning。

浏览器使用本地隔离验收页，不连接生产牌局。BrowserAct 技能依赖不可用，沿用现有内置浏览器，未安装新软件。本轮仅前端行为变更，未重跑后端套件。

## 发布

已发布到 `http://82.156.207.98:5173/`，入口、JS/CSS、图集逐项 HTTP 200 且 SHA256 与本地完全一致；规则和房间接口也为 200。

入口 SHA256：`57fe1dcfa3b620b31088b43f56d65223d576cc994d5d25dc750e04c0c21f192a`；JS `index-tMcLslvO.js`，SHA256 `ec96bab893fd01be053a4845b8d063f2da031b8ab5569c6269686cdb8c4648ed`。CSS 和图集沿用原资源。

发布备份：`/home/leo/mahjong_20260822/backups/draw-selection-space-20260911`。后端 PID 前后均为 `1521168`，jar 哈希未变，后端/nginx/FRP 持续 active；没有重启服务或操作生产牌局。
