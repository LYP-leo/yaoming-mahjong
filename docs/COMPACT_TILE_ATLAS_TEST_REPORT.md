# 紧凑牌面图集无损优化测试报告 · 2026-09-08

按编码前 [优化计划](COMPACT_TILE_ATLAS_PLAN.md) 完成。本轮只搬运、打包、无损压缩图片及替换坐标，不重新绘制。后端、玩法、SVG 轮廓和红五滤镜均未修改。

## 体积与像素结果

| 素材 | 实际字节 | 尺寸 | 用途 |
| --- | ---: | --- | --- |
| 原始 PNG | 1,259,949 | 2224×759 | 保留为再生成来源，不再被正式应用导入 |
| 紧凑完整色彩 PNG | 590,828 | 1098×728 | 无损编码对比候选，不发布 |
| 紧凑 lossless WebP | 368,076 | 1098×728 | 正式发布，减少 891,873 字节 / 70.79% |

- 34 张牌原尺寸复制，9 列×4 行；每格 122×182，牌外额外保留 2px 原图采样保护区，避免缩小或旋转时采样到邻牌。有效牌面尺寸仍为数牌 118×177、字牌 117×178。
- `scripts/tile-atlas-source.json` 锁定原始裁切；`scripts/build-tile-atlas.mjs` 直接逐行复制 RGBA，生成图集与坐标 JSON，不做缩放、调色、量化、锐化或描摹。正式运行只依赖生成资产。
- 原始 PNG SHA-256：`8c99333dcce4e3ce8d882b24699ed7d5fcf793a3e76338a70beeb09daedff269`。
- WebP SHA-256：`9180144ad25e07ebce59c75c11f81b8d81df44d7bbdc9157928b8ad4b679e0d8`；完整解码 RGBA SHA-256：`5fbe80f70e9032bd1965bb6a0a6c30c1a4bb4c4196f633c9b203f4e273621f6d`。
- 34 张牌及保护区与对应原图区域逐像素差异均为 0，候选 PNG 和 WebP 的完整 RGBA 也均等于打包前缓冲区。
- 初次透明空白区方案触发了 WebP 对不可见透明像素 RGB 的归一化，未满足整幅字节校验；改用不会显示的白色空格后，整幅 RGBA 校验也通过。实际牌面及保护区始终复制自原图。

## 自动化

- 本地 `npm test`：15 个文件、386 项全部通过（23:02）。新增像素一致性专项 37 项，覆盖原始源窗口、无损输出、逐牌/保护区和格间边界；当前共享牌面/图集测试 121 项，下游呈现 5 项、旧版编辑与状态呈现 4 项。
- 保留结算实体标记、暗牌隐私、复盘只读、听牌不写游戏动作、规则编辑、自动摸牌和推送等已有回归断言，没有用减少功能覆盖来通过测试。
- `npm run atlas:check` 成功；`npm run build` 已自动执行该校验，然后 TypeScript 和 Vite 均通过。正式构建只有一个 WebP 牌面资产，没有原始大 PNG，也没有开发预览页。
- 共享 `MahjongTileFace.vue` 发布前后文件 SHA 相同：`cbf582505d54892475f4c3281bc47f74808e2415c9cc8d835c651ab03acf9abe`，其比例、圆角、颜色滤镜与可访问性保持原样。

## 本地浏览器

browser-act/uv 不可用，仍用现有应用内浏览器，无额外安装或牌局写操作。

- 34 张目录逐行目检，以及七字牌和七筒/七索的大牌、正常牌、小牌目检通过，没有看到边缘串色、截断或图案变化。
- 红五兼容变体、横置供牌、加杠叠放、暗杠与选中显示正常。DOM 实际图源为 `mahjong-tile-atlas.webp`，尺寸为 1098×728；重复 ID 为 0，牌背中的 image 为 0。
- 默认浏览器 DPR 为 1.75。手机 390px 视口下内容宽/滚动宽均为 375px，无横向溢出，三尺寸七牌可辨认；测试后恢复视口。未宣称覆盖所有浏览器和 DPR。

## 发布

- 发布前备份：`/home/leo/mahjong_20260822/backups/compact-atlas-20260908/before-compact.tar.gz`，包含旧坐标、测试、预览、依赖清单、生产产物和 README。
- 发布前后端 PID：`1843067`，启动时间 `2026-09-08 20:56:27 CST`。计划仅更新前端，保留旧哈希资源供已打开的旧版本使用。

截图版本曾发布；随后用户要求停止使用截图，未完成的后续截图验收不再继续。本文仅为历史记录，不代表当前牌面；当前结果见 `UPSTREAM_TILE_ASSETS_TEST_REPORT.md`。
