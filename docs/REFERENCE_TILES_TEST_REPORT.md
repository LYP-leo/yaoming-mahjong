# 新参考图截取牌面测试报告 · 2026-09-08

## 实施与素材

- 按编码前 [计划](REFERENCE_TILES_PLAN.md) 执行用户最新的“直接从新图截取”要求，替代此前矢量重绘版本；本轮不改后端、规则或房间。
- 原图实际 2224×759，1,259,949 字节。源资产：`frontend/src/assets/mahjong-cropped-reference-20260908.png`，SHA-256 为 `8c99333dcce4e3ce8d882b24699ed7d5fcf793a3e76338a70beeb09daedff269`，与用户原文件逐字节一致。
- `tileReference.ts` 定义 34 个互不重叠的独立牌张窗口；`MahjongTileFace.vue` 使用原图直接裁切显示，不生成或描摹图案。保持源比例及蓝色牌边，圆角窗口排除外部桌布；无角标、无系统字体、无外部图床。
- 游戏 H5/H6/H7 分别映射原图下排第 3/2/1 张，即中/發/白，白板为原图纯白。旧版红宝牌使用限定在牌内的着色兼容层，蓝边不变；原图没有红五，不将其称为原样截取。
- 兼容着色保持纯白像素不变；截图内接近白色的压缩纹理可能轻微偏红。这不是普通 34 张牌的改色，普通牌不使用任何滤镜。

## 本地浏览器目检

browser-act/uv 命令未安装，使用现有应用内浏览器验收，没有安装工具或修改用户浏览器设置。完成后恢复临时手机视口。

- 全套 34 张目录逐行查看：万、筒、索与七种字牌映射正确，未看到邻牌或说明文字混入，形状无拉伸；七筒斜排、七索顶端红竹来自新图。
- 七种字牌以及七筒/七索的 91×134、43×61、24×34 三尺寸目检通过。截图本身清晰度有限，不宣称矢量无损放大。
- 普通五/红五对照：图案着红、蓝边保留，未加徽章。横向供牌、加杠叠放、暗杠背面及结算展开样例正常。
- 选中高亮、摸牌的牌外标记保留；暗牌只有背面。没有发起牌局或写入用户状态。
- DOM 验证：目录 34 个 SVG / 34 个裁切窗口 / 1 个共同图片来源，重复 ID 为 0，无 SVG text/foreignObject。
- 手机 390px 视口：内容宽/滚动宽均为 375px，未横向溢出；暗牌中的 image/牌面元素为 0。

## 自动化与发布

服务器发布前备份：`/home/leo/mahjong_20260822/backups/reference-tiles-20260908/before-crops.tar.gz`。备份包括本轮相关原组件、测试、预览、README 与原生产产物，可回滚。

发布前后端：PID `1843067`，启动时间 `2026-09-08 20:56:27 CST`。本轮仅发布前端。

- 本地最终全量 `npm test`：14 个文件，348 项全部通过（22:27）。当前裁切专项 120 项、下游呈现 5 项、规则编辑/外部标识 4 项；其余为已有游戏与兼容回归。历史字形路径测试仍保留供回溯，但旧字形组件不进入当前正式牌面。
- 调整过程中移除了与最新用户要求相冲突的“禁止 image/必须矢量”断言，替换为原图校验、精确窗口和资源约束；首次回归还修正了测试对 Vue 自动添加 scoped 属性的过严全等比较。没有因此移除隐私或游戏行为断言。
- 首次完整类型构建发现新增下游测试中 `data-face` 可能缺失的 TypeScript 检查错误；已加入显式守卫，缺少牌值仍会使测试失败，不弱化断言。修正后本地及服务器 `npm run build` 均通过。生产包包含新图，开发预览页不打包。

- 服务器最终重跑：14 个文件、348 项通过（22:27），随后构建通过。先复制带哈希资源，再原子替换入口 HTML；没有删除旧资源或重启后端。后端仍为 `active`，PID/启动时间与发布前相同。
- 新主包 `index-DysSF8nx.js`；旧版入口包 `LegacyApp-B2WoZc2-.js`；图片 `mahjong-cropped-reference-20260908-BcDH4NtZ.png`。首页和全部 5 个 JS/CSS/PNG 资源的公网 HTTP 状态均为 200，字节内容和 SHA-256 与本地、服务器构建一致。
- 公网应用内浏览器刷新后大厅“已连接”，规则手册实际显示新牌面。DOM 为 27 个正式规则用牌、27 个 reference-crop，图片指向新版带哈希资源，白板窗口为 `1622 342 117 178`。
- 公网截图中实际核对万子、全套筒/索和東南西中發白：没有旧字形、说明文字或相邻牌混入；蓝色牌边保留。未创建或操作任何真实牌局。
