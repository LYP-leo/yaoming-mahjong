# 九数齐不得含字牌：测试报告

日期：2026-09-09。对应编码前计划 NINE_RANKS_NO_HONORS_PLAN.md。

## 修改范围

- 计番器按完整和牌（暗手、雀头、全部副露）判定无字牌后才授予九数齐；仍为 4 番，保留每种数值只归属一个面子/雀头的限制。
- 规则目录同步明确无字牌，前端规则/结算读取真实服务端说明和番数；不改布局、牌面或其他独立番种。
- 审查发现旧响应窗口中失效 WIN 可能在结算报错后卡窗，已补计划并安排动作展示、恢复、最终仲裁资格复核。已结束结算不追溯改分。

## 已执行测试

- 计番专项：107 项全通过，其中新增 48 项；覆盖 6 种在用字牌 × 雀头/闭刻/碰/明杠/暗杠/加杠共 36 例，北风牌组外非法 6 例，纯数牌不同副露正向 5 例及规则目录 1 例。每例检查其他番种和实际总番，而非只看九数齐有无。
- 前端：20 文件、489 项全通过（新增 6 项规则展示/搜索/服务器结算回归）；素材校验、TypeScript 和 Vite 构建通过。index/JS/CSS/SVG 哈希均与上一版一致，没有前端运行时代码改动。
- 跨模块与旧窗口新增 14 项：错误含字九数齐不再够自摸/点和门槛，听牌/机器人一致；纯数牌开手恰好 4 番仍可和；含字牌但其他番够番仍和；旧失效 WIN 清理不改倒计时、手动旧 WIN 请求零突变拒绝、已提交无效 WIN 不阻碍另一家过/碰、仲裁复核不再卡窗，已结束结算不追溯改分。先写测试时 6 个窗口例复现旧错误，修复后定向 89 项全通过。日志 `integration-before-window-fix.log`、`integration-final.log`。
- 最终后端：JDK 21 隔离 clean package，21 套件、439 项全通过，0 失败/错误/跳过；40 个输入文件逐个 SHA 与主工作区一致。日志 `backend-build.log`。与前端合计 928 项通过。
- 实际 HTTP 三局全部通过：错误含字九数齐仅 1 番，WIN 返回 400 且版本/阶段/结果不变，之后正常摸牌继续；纯数牌九数齐 4 番＋门清 1 番，5 番点和结算；含字牌的另一手以番牌 1＋门清 1＋混一色 2 正常 4 番结算，不含九数齐。两局胜利的三家结算、14 张赢家手牌、总分 30 与牌谱结果一致。日志 `http-smoke.log`，测试服务器已停止，未碰线上对局。

## 已发布构建

- JAR SHA256：`ae73ccbe7349b536057ab515d7fd770cd58f5e0992e14b1a63d4a7b634333cfc`。
- 前端 index SHA256：`25ac667c78d8066ce34d38af3335a9d71e962c3d1069ec57392693a751ce7e51`，与上一版相同；九数齐新说明由真实规则 API 提供。

## 证据和发布范围

证据目录 `artifacts/nine-ranks-no-honors-20260909/`，含编码前源码备份 `source-before.tar.gz`、前端测试/构建日志、计番器隔离构建目录 `backend-scoring/`。

实际 HTTP 脚本 `scripts/yaoming-nine-ranks-smoke.mjs` 只接受本机 18093，固定独立存档，不向线上写测试状态；覆盖错误九数齐不足番拒绝且牌局继续、纯数牌九数齐结算、含字牌但其他番够和的正确结算，以及三家结果/牌谱一致。

本次沿用用户已明确允许中断对局直接重启的授权，仍先备份完整数据并保留房间与牌谱，不更改 FRP/Nginx。

## 本地更新与线上验收

- 本地确认房间为空后备份更新，后端 PID 35892，JAR 与上述测试产物 SHA 完全相同，新规则/房间接口 200、旧版接口 404；房间仍为空。日志 `local-update.log`。
- 源码发布包 SHA256：`bcfd9bceb156bcb9ad309d6138e7499fcc914eaf525e0349640a20fd49c09f95`。发布脚本 `scripts/deploy-nine-ranks-no-honors.sh` 校验源包条目白名单、旧/新产物哈希并备份，使用用户允许直接重启的参数；此次实际发布前两次检查均为 4 房间、0 活动局。
- 线上后端已重启至 PID 2942355，active/running、NRestarts=0；安装 JAR 与本次已测试 JAR 的 SHA256 相同。4 个原房间的 ID、状态与人数重启前后一致，部署期间数据文件校验未改变，房间和牌谱保留；Nginx、frpc_tencent 正常运行，未修改配置。
- 公网规则 API 返回 JIUSHUQI 4 番，说明为“完整和牌（含雀头和所有副露）不得含字牌；包含 1 至 9，每种数值只能出现在一个面子或雀头中。”取消振听的上一轮规则保持。公网首页 HTTP 200，约 0.097 秒。
- 浏览器实际刷新公网已有页面并点击“规则手册”，九数齐卡片准确展示上述说明及 4 番，无页面告警。未创建或改动其他玩家的牌局。
- 服务器恢复位置：`/home/leo/mahjong_20260822/backups/nine-ranks-no-honors-20260909/`，包括原源码、公开静态资源、停止状态的完整数据、原 target/dist。历史已结算结果不追溯改分；提权 SSH 会话已退出。

当前状态：本地和线上均已更新，前后端 928 项全量测试、三场隔离真实 HTTP 场景及线上规则/浏览器验收已完成。
