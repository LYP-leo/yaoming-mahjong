# 要命麻将离线强化学习训练包交付记录

日期：2026-09-12。

按先计划后编码流程实施，计划见 `RL_TRAINING_PACKAGE_PLAN.md`。新增独立 `training/`，没有修改后端业务规则或前端，没有更新线上网站。

交付内容：

- 使用现有 Java 规则引擎的离线三人/四人模拟器，私有观测与合法动作编码。
- 3060 Ti 8GB 小型策略/价值网络、masked PPO、冻结对手自我对弈。
- 规则机器人监督模仿预热、固定种子轮换座位评估。
- 原子检查点、历史快照、安全停止、恢复优化器与随机状态、规则/协议兼容性检查。
- Windows Python/PyTorch/Java 环境搭建文档和局部虚拟环境安装脚本。
- 预编译模拟器与规则源码快照，自包含可携带 ZIP。

验证结果：

- Python 全套 40 项通过，无失败、无跳过，Java 21 环境最终回归耗时 53.75 秒。
- Java 10 场完整三/四人对局及隐私、吃碰杠、优先级、守恒、复现断言通过。
- 1 线程与 4 线程模拟器 8 环境逐帧结果一致。
- 实际执行模仿、三/四人 PPO、保存、续训、完整评估；净分差奖励和梯度通过。
- 独立解压目录无原项目路径依赖，CPU 设备检查、训练、续训、4 场评估及源码重建全部通过。
- `git diff --check` 通过。

详细说明位于 `training/TEST_REPORT.md` 与 `training/INSTALL_WINDOWS.md`。

边界：本机没有 NVIDIA GPU，仅 CPU 实测；目标机 cu126 安装、3060 Ti 实际显存和吞吐需用户自检。没有训练长期模型，也不声称达到某棋力。共享 GPU 内存不计入 8GB 专用显存预算。没有将训练模型接入线上机器人。
