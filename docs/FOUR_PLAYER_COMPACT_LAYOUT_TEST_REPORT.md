# 四人牌桌轻量压缩测试记录（2026-09-12）

## 结果

- 每家固定区域：158px → 146px，减少 12px。
- 四人牌桌：桌面 672px → 614px（减少 58px）；手机 662px → 614px（减少 48px）。
- 四人页主体上下边距：桌面 16px → 10px；手机 10px → 6px；房间标题下间距 12px → 8px。
- 连同页内边距，桌面总纵向占用减少 74px，手机减少 60px。没有强制承诺任意小屏幕一屏显示完整页面；保留正常整页纵向滚动，不裁切操作区。
- 三人桌面 506px、手机 498px、每家 158px，保持不变。

## 自动化验证

- `npm.cmd test`：36 个文件、854 项测试全部通过。
- `npm.cmd run build`：牌面资源完整性、vue-tsc 类型检查、Vite 生产构建全部通过。
- `git diff --check`：通过。
- 更新四人固定行高、普通/本人边框补偿、80px 公开轨道、计分固定第 5 行、行动标记固定 14px、底部手牌/动作区不缩小的断言；新增四人切回三人恢复原尺寸测试。
- 原有三/四人复盘、键盘、听牌与布局回归全部通过。本次不修改后端代码，未重复运行后端测试。

## 本地真实浏览器验证

使用独立 `stable-table-preview.html` 合成布局样例，不访问后端、不创建真实房间。browser-act/uv 命令缺失，改用现有内置浏览器；没有安装依赖。

| 视口 | 四人桌高 | 各行高度 | 压力切换后固定锚点 | 边界/内部滚动条 |
| --- | --- | --- | --- | --- |
| 1366×768 | 614px | 146px × 4 | 不变 | 无越界、无新增 |
| 1280×720 | 614px | 146px × 4 | 不变 | 无越界、无新增 |
| 390×844 | 614px | 146px × 4 | 不变 | 无越界、无新增 |

场景包括：每家 60 张压力牌河（显示最近 24/16 张）、四组副露含加杠、摸切标记、13/14 张切换、行动玩家切换、长提示、13 个鸣牌动作与听牌内容切换。逐项比较玩家区域、计分、副露、牌河、手牌面板、动作区及听牌区的真实坐标，完全不变。

- 加杠叠牌、副露标签、计分、暗牌和可见牌河牌面均在玩家区域内；没有公共区或动作区内部滚动条、整页横向溢出。
- 暗牌/牌河比例相对 19:26 的误差小于 0.00034（亚像素取整）；私有手牌保持桌面 38×52px、手机约 29×39.675px。
- 本人北位、只有本人入座的等待场景仍保留四个 146px 区域。
- 桌面超长昵称和房间名称压力样例：无牌桌越界、内部滚动条或横向页面溢出。
- 切回三人后，桌面原始几何值完整恢复；手机恢复 498px 桌高与原边距。
- 浏览器错误日志为空。

## 发布校验

已发布到 `http://82.156.207.98:5173/`，公网校验入口、JS、CSS、牌面图集均 HTTP 200，SHA256 与本地构建完全一致；规则和房间列表接口 HTTP 200。

- 入口 SHA256：`2eaf641008af91132bc141dc1f6587d108af2176a8432ca3b9cdb91b5263563b`。
- CSS：`index-BbsVmAan.css`，81,553 字节，SHA256 `c164d05d39c5009b18cf010c570caa793e890cf9bb0f73d80f9825ea273db948`。
- JS：`index-B58yNiAF.js`；内容哈希与发布前完全相同，说明业务代码未变。牌面图集内容也未变。
- 后端发布前后 PID 均为 `3012626`，JAR 哈希未变化；后端、nginx、FRP 服务保持 active。
- 前端备份：`/home/leo/mahjong_20260822/backups/four-player-compact-20260912`。
- 页面交互与几何验证在本地隔离浏览器完成；公网验证为资源及接口 HTTP/哈希校验，没有在真实站点创建测试房间或操作玩家座位。

部署只更新前端样式、相应测试文档与生产静态资源。保留旧的哈希资源供已打开页面使用，入口文件最后原子替换；不重启后端、不操作房间数据。
