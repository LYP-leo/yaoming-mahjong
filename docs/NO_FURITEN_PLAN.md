# 要命麻将取消振听限制计划

日期：2026-09-09，编码前记录。

## 已确认规则与范围

用户明确确认：要命麻将没有舍牌振听，也没有“放过有效点和后，自己下次摸牌前不能点和同种牌”的限制。两种限制都取消。点和仍必须满足既有合法和牌结构、4 番起和及当前响应窗口等条件，计分、响应优先级、纯摸切托管策略不变。

1. 引擎不再以自己打过或放过同种牌拒绝点和，不再写入临时过和禁和状态。旧存档字段可保留兼容，但不得影响和牌资格。
2. 听牌辅助不再排除自己曾打过、刚假设打出或曾放过的同种牌；保留公开可见张数计算与隐私边界。
3. 智能机器人评估所有合格点和进张，不再把对手打过的牌当作绝对安全牌。真人托管仍只摸切、响应过。
4. 同步规则接口、前端规则说明、提示及相关测试样例，避免继续传播错误规则。
5. 审查补充（该补丁编码前记录）：旧存档若恰处于尚未过期的响应窗口，原存储选项可能已按错误限制删掉 WIN，甚至未把该座位放入选项。恢复时为尚未响应的座位修复点和选项；不得撤销已提交动作、延长时限或复活过期窗口。验证旧两类快照格式、遗漏座位、已有选项及截止时间。

## 验证与发布

- 回归覆盖曾打过、曾放过、过后尚未摸牌、旧持久化状态、实际响应和牌及结算；不足 4 番仍不可点和。
- 验证听牌建议、0 张显示计数、机器人判断与信息隔离、托管响应仍为过。
- 运行前后端全量测试和构建，必要的隔离服务 API 检查、浏览器规则页面检查，结果记录在 NO_FURITEN_TEST_REPORT.md。
- 先备份，发布前检查服务器无进行中牌局；如有活动牌局不打断。保留房间、牌谱、FRP 和 Nginx 配置，测试使用自建隔离数据。

## 发布授权补充（2026-09-09，脚本修改前记录）

用户最新明确允许中断所有对局、直接重启。本次发布使用显式一次性参数允许在活动牌局期间切换，默认脚本仍保护活动牌局。保留源码、静态资源和停止服务后的完整数据备份，不清空房间或牌谱。活动牌局可能因原有计时和机器人调度正常推进，因此该模式不要求重启前后阶段逐字相同；仍校验替换前后数据文件未被部署修改、服务健康、新规则接口和线上静态资源。更新后实际浏览器复核并补报告。
