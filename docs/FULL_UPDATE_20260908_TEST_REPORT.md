# 前后端联合更新测试报告 · 2026-09-08

## 结果

按用户要求先停止后端，再重新测试、构建并发布前后端，随后启动服务。已恢复公网访问，原有三个要命麻将房间摘要与停服前一致。本轮没有创建测试房间、加入用户房间或发送游戏动作，没有修改 Nginx/FRP。

## 发布过程

- 20:53:48 CST：Spring 日志确认优雅关闭完成，MainPID=0、8080 无监听。
- 停服备份：`/home/leo/mahjong_20260822/backups/full-update-20260908-2100/stopped-before-update.tar.gz`（服务器，权限 600）。包含原源码、JAR、前端、部署文档和完整 `backend/data`，可用于程序回退；不自动回退或覆盖之后的用户数据。
- 本地与服务器后端 `pom.xml` + `src` 共 80 个文件逐项 SHA-256 相同，因此不做无必要的源码覆盖。使用服务器 JDK 25 从干净构建目录重新打包，目标仍为 Java 21；运行仍用 Java 21。
- 后端新 JAR SHA-256：`e254048c4179bcab6f28b72771acb9a67aa5f56a71720d81e5791ea00641857a`。
- 前端重新测试、构建、发布。主脚本 `index-NdcqkG9B.js`、样式 `index-CADmadaI.css`；与上一轮矢量牌面版本一致，保留旧哈希资源。
- 服务增加 `SuccessExitStatus=143`：旧配置把 Java 正常响应 SIGTERM 的退出码误标为 failed，日志中实际优雅关闭已完成。新配置经 `systemd-analyze verify` 验证，安装并 daemon-reload 后生效，未改变其他运行参数。
- 20:56:27 CST：启动新后端，PID `1843067`；20:56:31 应用启动完成，8080 正常监听，`active/running`、NRestarts=0。

## 测试与访问验证

| 检查 | 结果 |
| --- | --- |
| 后端 `mvn clean package` | 364 项，失败 0、错误 0、跳过 0；BUILD SUCCESS |
| 前端 `npm test` | 14 个文件、312 项全部通过 |
| 前端 `npm run build` | TypeScript 与 Vite 构建成功 |
| 服务配置语法、加载状态 | 通过；SuccessExitStatus=143 已加载 |
| 公网首页与新主脚本 | HTTP 200，首页指向最新构建，脚本含矢量牌面 |
| 公网 `/api/yaoming/rules` | HTTP 200，20 个番型、27 种牌 |
| 源站与公网房间列表 | HTTP 200，原有 3 个房间 ID、状态及人数与停服前一致 |
| 公网 SSE 缺鉴权探测（虚构房间，无用户身份） | HTTP 400 JSON，未错误升级为 SSE 或返回 500 |
| 后端启动日志 | 正常启动；检查范围内无启动异常，未发生自动重启 |

房间恢复摘要：`864106`（MATCH_END、2 人）、`016643`（MATCH_END、1 人）、`262741`（MATCH_END、3 人）。不在报告中记录手牌、牌墙或恢复凭据。

停服后及测试构建完成、重启前的要命麻将数据 SHA-256 均为 `b18840cdf7dd4844854a610227ca3c0984f8604bf765fe5274119233feb4eea8`，证明构建没有覆盖该数据文件。

重启进入运行状态后，服务继续持久化，文件校验和不再作为停服前后字节相等的验收条件；本轮核验房间摘要恢复及备份完整性，不声称已逐字段复核全部历史牌谱。

## 说明

- 后端测试中的损坏快照、落盘失败日志是临时测试目录中的故障注入，最终 364 项全部通过；并非线上数据错误。
- Maven/JDK 输出了现有 Mockito 动态代理和 Unsafe 弃用警告，不影响本次测试或 Java 21 运行；本轮不升级依赖。
- 本次为联合重新构建/发布与启动验收，没有新增玩法。未再次运行会创建牌局的 HTTP smoke 脚本；牌面浏览器验收记录见 [矢量重绘报告](YAOMING_TILE_VECTOR_TEST_REPORT.md)。
