# GitHub 工程同步计划（2026-09-15）

## 目标与前置确认

- 用户要求同步当前工程，并先检查 GitHub 账号。
- 已通过认证接口确认账号为 `LYP-leo`（用户 ID `50077176`）。
- 目标 `LYP-leo/yaoming-mahjong` 已存在，为私有仓库；账号具有 push/admin 权限；初次检查仓库大小为 0。
- 本机 Git 没有已登录的 GitHub 身份，采用已经授权的 GitHub 连接器完成上传，不要求再次登录，不读取或复制连接器凭据。

## 同步范围

- 以本地 Git 已跟踪文件及未跟踪但未被忽略的文件为候选，上传 Java 后端、Vue 前端、牌面原始素材和许可、文档、部署脚本、离线训练源码。
- 排除 `.git`、密钥、环境私密配置、真实房间/牌谱数据、依赖目录、构建产物、模型权重、测试归档及旧截图。
- 保留现有游戏规则和代码，不在同步任务中修改功能；不操作线上服务器或对局。

## 执行流程

1. 复核候选文件中的敏感内容、二进制文件及大小，运行前后端测试与前端构建。
2. 按现有 `.gitattributes` 取得 Git 规范化后的文件内容及 blob SHA，记录同步清单。
3. 再次确认目标为空仓库后，初始化默认分支，使用 Git 对象接口分批上传文件并生成完整源码快照提交。
4. 更新 `main` 时不使用强制覆盖；若发现远端出现未预期提交，停止并检查，不覆盖其他更新。
5. 通过远端递归文件树逐文件比对路径、模式和 blob SHA，确认仓库仍为私有。
6. 记录测试、远端提交和校验结果。本地原有提交历史、暂存区和未提交工作保持原样；连接器同步不冒充本机 `git push`。

## 验收

- 认证账号正确、目标私有、没有误传敏感数据。
- 远端完整文件清单与规范化本地快照逐一对应。
- 前后端测试和前端构建结果明确记录；用户得到仓库地址和提交标识。
