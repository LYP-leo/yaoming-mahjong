# 稳定牌桌布局与仅保留新版测试报告

日期：2026-09-09。对应编码前计划：`docs/STABLE_TABLE_NEW_ONLY_PLAN.md`。

## 当前结论与发布状态

本地后端隔离构建及回归、隔离 HTTP 全流程、前端回归及最终正式构建、桌面/窄屏浏览器专项验收已完成。此次验证覆盖静默听牌显示、布局稳定性和旧版运行代码清理，不代表已经消除项目全部缺陷。

**线上联合发布及发布后复验已完成。** 2026-09-09 01:47（北京时间）执行 `scripts/deploy-stable-new-only.sh`，先备份再联合更新后端、Vue 正式构建和 Nginx 退役入口；公网浏览器与 HTTP 已验证新版。本地 8080 也已切换到相同已测试 JAR。

## 测试范围

- 听牌区域无候选时不显示说明文字；有候选时保留牌面与余张数。刷新期间保留原结果，避免加载提示及不必要的 DOM 重建造成闪动。
- 听牌区域、玩家区、中央区、自家操作区保持固定空间；牌河、副露、长提示和多操作溢出时在区内滚动。
- 移除旧版前端入口、页面、状态和接口，以及旧版专属后端 API、规则、服务、配置和依赖；保留新版需要的共享代码。
- 新版要命麻将的真实 HTTP 对局、听牌、实时通知、牌谱、结算和退出流程。
- 历史文档、备份、房间数据与有效旧快照兼容逻辑不属于本次运行代码删除范围。

## 自动化与构建结果

| 项目 | 环境 / 方式 | 已完成结果 |
| --- | --- | --- |
| 后端隔离构建 | JDK 21；仅复制当前 `backend/pom.xml` 与 `backend/src`，在隔离目录执行 Maven `clean package` | 15 个测试套件，278 项通过；失败、错误、跳过均为 0；打包成功 |
| 前端最后完整回归 | 清理旧手绘字形及其 30 项专属测试后执行完整测试 | 16 个测试文件，451 项全部通过 |
| 前端最终正式构建 | 最终清理后的 `tiles:check`、类型检查及 Vite 构建 | 全部通过；主线程已核对最终产物 |
| 新后端 JAR 内容审计 | 检查实际隔离 JAR 的类与依赖条目 | 无旧版规则/服务/控制器/模型类；无 Spring WebSocket、Messaging 或 WebSocket starter 依赖 |

后端隔离构建目录：

`artifacts/stable-table-new-only-20260909/backend`

测试 JAR：

`artifacts/stable-table-new-only-20260909/backend/target/mahjong-server-0.1.0.jar`

- 大小：22,459,505 字节。
- SHA-256：`4ccd814ddce7260c67fcbd41e9e4bec85db373505521caedd1c779030e4ca360`。
- 隔离复制的 34 个构建输入文件与工作区对应文件逐一核对，哈希一致。
- 没有使用原 `backend/target` 的旧类作为构建输入，避免已删除类因增量构建残留进入新 JAR。

最终前端 `dist/index.html` SHA-256：`248845edc9705ee14ab5a39ebd5b0fe86a6085a9b9414d7ebdeded51efff7b43`。

| 正式静态资源 | 字节数 |
| --- | --- |
| `assets/index-BL9ZKZ5b.js` | 202,514 |
| `assets/index-4xtHB0ci.css` | 52,956 |
| `assets/mahjong-graphic-atlas-C-odaEvj.svg` | 250,050 |

图集 SHA-256 保持为 `4e4337f4aa7f9b0ed43134007c10e4cdeb7b42da66ffbd4ff8457af49433ef0f`，未因清理旧版而改变现用牌面资源。

### 后端仅新版专项

新增 `YmOnlyApplicationTest`，使用真实 Spring Boot 应用扫描和隔离临时存储，新增 25 项测试；原有新版 253 项测试保留。

验证内容包括：

- 旧版类及旧 WebSocket/Messaging 类不可加载。
- 注册的游戏 API 路径属于 `/api/yaoming`。
- 22 个旧版路径与请求方法组合返回 404，包含原房间、规则和 WebSocket 入口。
- 新版规则读取、创建房间、查看房间、准备和听牌请求可用。
- 保留新版所需的 CORS 和请求参数校验。

## 隔离 HTTP 两局全流程

使用上述新 JAR 启动独立的 `127.0.0.1:18089` 实例。启动前确认端口空闲，独立数据与日志均放在：

`artifacts/stable-table-new-only-20260909/smoke-18089`

执行：

```powershell
node scripts/yaoming-smoke.mjs http://127.0.0.1:18089 --features
```

结果：通过。脚本仅创建和操作自己的测试房间。

| 对局 | 实际结束方式 | 结算后点数 |
| --- | --- | --- |
| 第一局 | 4 番自摸 | `[18, 6, 6]` |
| 第二局 | 6 番点和，整场结束 | `[24, 6, 0]` |

实际动作覆盖：摸牌 86 次、弃牌 97 次、放过 10 次、暗杠 1 次、和牌 2 次、确认结算 3 次、碰 5 次、吃 6 次、明杠 1 次。

- 接收 216 条 SSE 事件。
- 验证手牌隐私、108 张实体牌守恒、点数总和 30、和牌及结算结果一致。
- 验证两局牌谱可读取，退出后历史牌谱仍可访问。
- 验证通知不泄露身份令牌，退出后测试房间清理完毕，房间列表恢复为空。
- 真实 HTTP 请求 `/api/rooms`、`/api/rules`、`/api/rules/presets`、`/ws/info` 均返回 JSON 404，并未被异常处理转换为 500；`/api/yaoming/rooms` 返回 JSON 200。
- 测试结束后核验进程命令行并仅停止该隔离实例；18089 端口释放，原有 8080 服务未停止、未重启。

## 浏览器专项验收

以下为主线程已完成的本地浏览器实际测量。布局压力场景使用隔离开发验收夹具，不将测试夹具当作生产入口发布。

### 听牌区域稳定与余张显示

在宽度 1280 px 的视口中比较无候选、有候选和异步刷新状态：

| 检查点 | 实测结果 |
| --- | --- |
| 无候选 / 有候选的区域高度 | 均为 184 px |
| 区域下方标尺纵坐标 | 均为 445.464 px |
| 相同结果延迟返回前后 | 既有牌面 DOM 节点保留，`tile-artwork-v-0` / `tile-artwork-v-1` 标识不变 |
| 余张标记 | `1 张` 与 `0 张` 均保留显示；0 张候选未因数量为零而消失 |

观察结果符合“后台更新、保留当前显示直至新结果可用”的目标；本次测量不以固定加载文案代替原候选。

### 桌面与窄屏牌桌压力布局

压力条件：各家 60 张弃牌、4 组副露、长提示、多项操作按钮，并比较托管切换前后。该夹具专门检验布局承载能力，并非宣称真实一局可以产生同样的超量牌数。

| 视口 | 牌桌高度 | 玩家区高度 | 中央区高度 | 自家区高度 | 压力前后结果 |
| --- | --- | --- | --- | --- | --- |
| 桌面宽度 1440 px | 646 px | 314 px | 104 px | 198 px | 各区域位置不变 |
| 窄屏 390 × 844 px | 600 px | 289 px | 100 px | 192 px | 各区域位置不变；超量内容内部滚动 |

补充最窄屏检查：320 × 700 px 视口中，页面 `scrollWidth` 为 305 px，`innerWidth` 为 320 px，未发生页面横向溢出。验收后已恢复桌面视口。

以上记录为对应视口下的实测值，不要求所有设备都使用同一像素尺寸。

## 删除范围、备份与恢复

本轮精确删除清单合计 67 个文件：后端 54 个、前端 13 个。后端 54 个包括 41 个主源码文件和 13 个旧版测试文件，删除前已逐一确认存在于备份中。

- 本地源码备份：`artifacts/stable-table-new-only-20260909/source-before.tar.gz`，341,468 字节。
- 后端删除清单：`artifacts/stable-table-new-only-20260909/backend-deleted-files.txt`。
- 保留新版共用的 `domain.Tile`；依赖审计确认无新版消费者的旧 `Meld` 类型一并移除。
- 保留 `WebConfig` 的有效 CORS 配置；旧 WebSocket 配置和依赖删除。
- `application.yml` 仅保留新版房间存储配置，默认仍为 `./data/yaoming-rooms.json`，可通过 `MAHJONG_YAOMING_FILE` 指定。
- 新版入口不再支持查询参数切回旧版；旧版专属前端页面、状态、API、样式及 STOMP/SockJS 依赖不进入新版运行链。
- 历史手绘字形实现已从当前前端移除；最终正式资源清单见上方构建结果。
- Nginx 的 `/ws` 配置是明确返回 404 的退役入口保护，不是仍在运行的 WebSocket 代理。

本次没有删除房间数据。需要恢复源码时，应先在独立目录解开备份并对照精确清单恢复，不应直接覆盖正在运行的新版数据或未完成对局。线上实际备份与恢复位置如下。

## 线上发布与复验

### 安全切换及备份

脚本在停服前重新读取真实房间列表，确认只有 4 个 `MATCH_END` 房间（864106、016643、262741、107661），无进行中的新版牌局。停服后备份一致性数据；新程序启动前确认数据文件哈希没有变化，启动后房间列表与之前逐字一致。

服务器恢复目录为 `/home/leo/mahjong_20260822/backups/stable-table-new-only-20260909`，权限限制为私有管理目录，包含：

- `source-before.tar.gz`：旧源码、构建目录、文档和部署配置。
- `public-before.tar.gz`：旧线上静态站点完整备份。
- `stopped-data-before.tar.gz`：停服后的全部 `backend/data`。
- `nginx-before.conf`、`target-before/`、`dist-before/`：旧 Nginx、后端可执行产物和前端构建。
- `retired-assets/`、`retired-original-site/`：下线的历史资源与原 `/var/www/mahjong` 旧站点。
- `source-current.tar.gz`：本次发布源码和正式资源。

新后端 PID `2139264`，`active/running`、`NRestarts=0`；JAR SHA-256 与隔离测试产物一致，为 `4ccd814ddce7260c67fcbd41e9e4bec85db373505521caedd1c779030e4ca360`。线上首页 SHA-256 与最终本地构建一致。Nginx 配置检查通过并重载；现有 `frpc_tencent.service` 保持运行，未改 FRP 配置。

逐项检查删除清单的 67 个路径，线上残留数为 0。正式站点和远端 `frontend/dist/assets` 均只含上表中的 3 个资源。前后端旧编译产物已退出活动目录；远端及本地旧 STOMP/SockJS 依赖均已清理。

### 发布后真实 Nginx 对局

在服务器运行已审查的 `node scripts/yaoming-smoke.mjs http://127.0.0.1:5173 --features`，使用实际生产 Nginx 入口创建自己的测试房间，不操作其他房间。

- 共完成 3 局：前两局荒牌流局 `[10,10,10]`；第三局 4 番点和，终场 `[20,10,0]`。
- 动作：摸牌 186、弃牌 196、放过 54、确认结算 6、吃 10、碰 4、加杠 1、和牌 1。
- 接收 SSE 463 条；三局牌谱、108 张实体守恒、总分 30、隐私、结算及退出后归档全部通过，脚本退出码 0。
- 自建测试房间已退出并清理。测试前后房间列表 JSON 完全相同，原 4 个房间保留不变。

### 公网 HTTP 和浏览器

从本机实际请求 `http://82.156.207.98:5173`：

| 路径 | HTTP 结果 |
| --- | --- |
| `/` | 200，418 字节 |
| `/api/yaoming/rules` | 200，JSON，20 番种 / 27 牌种 |
| `/api/yaoming/rooms` | 200，JSON |
| `/assets/index-BL9ZKZ5b.js` | 200，202,514 字节 |
| `/api/rooms`、`/api/rules` | 404，JSON |
| `/ws/info` | 404 |
| 前次旧构建 `/assets/index-DH2vQUVD.js` | 404 |

浏览器打开公网正式站点，实际显示“要命麻将 · 三人对局”、已连接状态、新版大厅和创建牌局入口；没有旧版入口。页面实际加载 `index-BL9ZKZ5b.js`，1280 px 视口下页面宽 1265 px，没有横向溢出。已将正式站点保留给用户，不把验收夹具作为正式入口。

公网浏览器本次抽查首页与资源；听牌异步不闪动和桌面/手机压力坐标比较是在前述本地真实组件夹具完成，并非公网真人牌局视频测试。生产构建与发布哈希一致，真实游戏链路另由生产 Nginx HTTP 全流程验证。

## 本地运行同步

确认原 8080 为本项目 Java 进程、且新版房间列表为空后，备份并仅停止该旧进程 `15912`。使用 JDK 21 启动相同已测试新 JAR，新 PID `25876`；新版规则与房间接口 200、房间列表仍为空，旧规则和房间接口 404。

本轮 artifact 目录额外保留 `local-runtime-before.tar.gz`、`local-data-stopped.tar.gz`、`local-target-before/` 和新进程日志。全部数据文件保留，没有创建本地真实房间。

恢复注意：本地旧磁盘 `target` 在本次之前已被构建覆盖为不含 `Main-Class` 的普通 JAR，运行前旧 Java 进程仍持有之前加载的程序。此次移动的 `local-target-before/` 可恢复原磁盘文件，但不能直接据此 `java -jar` 重启旧版；如确需恢复旧版可执行环境，应使用更早的完整可执行 JAR 备份。当前新版运行健康。

## 限制与未作结论的事项

- 浏览器记录覆盖指定桌面和窄屏尺寸，并未覆盖全部浏览器、缩放倍率、设备输入方式或网络环境。
- 两局真实 HTTP 对局证明了本次脚本路径及所列动作可用，不等同于穷尽所有随机牌序、番型和多人竞争时序。
- 布局压力夹具与真实对局测试分开执行；两者共同提供功能及视觉证据，但不应混为同一场景。
- 自动化通过不构成“所有 bug 已消失”的保证。生产资源、公网行为及发布安全性已按上述范围确认，未宣称覆盖无限牌序或全部设备。
