# 《要命麻将规则集(2)》更新测试报告

日期：2026-09-11

## 依据与范围

对应 [更新计划](RULEBOOK_2_UPDATE_PLAN.md)。源文件 SHA256：`035c0d8708dec106c15bdc56604815d458bcc08a0ad17b052dce04fb50c7711c`，封面仍为 26.9 LTS。

完整读取 DOCX 正文和两张表；未发现附图、批注、脚注或修订插入/删除。更新平和 1 番、门清 2 番、清全带幺 3 番，移除断幺；纯字牌不再叠清一色。计番、听牌、真人合法动作、机器人估分、结算和规则手册同步。花牌玩法与赛事积分公式仍未提供，未自行编造。

## 自动化回归

| 检查 | 结果 |
| --- | --- |
| Java 21 / Maven `package`（含全部测试） | **480 项通过**，0 失败/错误/跳过，成功生成 Spring Boot JAR |
| Vue / Vitest 全量 | **23 个文件、549 项通过** |
| 前端生产构建 | 牌面素材校验、`vue-tsc -b`、Vite 全部通过 |
| `git diff --check` | 通过 |
| 独立本机 HTTP 验证 | **7 个场景通过**，使用实际构建 JAR，非 Mock HTTP |

首轮后端存在 18 个旧番数断言失败；逐条核对规则后更正旧断言，不删除失败测试。原依赖断幺成立的牌例保留为新版不足 4 番负例；剩余张数测试改用真实平和牌，保留 0/1 张、四张可见、ID 去重及他家暗手不可见的原测试目的。

重点新增覆盖：

- 平和的数牌雀头、159 万顺子、允许吃、排除字雀/刻子/明暗杠、多种拆分取高分。
- 暗杠仍门清 2 番；自摸可叠不求人；风龙只能叠指定项目。
- 清全带幺 3 番的起和边界、字一色不再叠清一色及排除番种。
- 听牌显示番数与真正可执行和牌一致；低于 4 番的 `WIN` 被拒绝且状态不变。
- 机器人使用新版番值，移除旧断幺与字一色旧估分；已有机器人行为及对局回归通过。
- 快照指纹迁移只递增一次版本，旧请求拒绝；响应重新评估，不延长时间、不撤销过牌。
- 旧结算真实例：原门清 1＋断幺 2＋不求人 1＝4，支付后 18/6/6；新版同手仅 3 番，但历史结算、完整牌谱和终帧在迁移/再次重启后完全保持旧记录。
- 规则手册再次打开刷新；等待/请求失败期间保留已有牌卡，不闪烁；结算与牌谱组件保留历史番数。
- 前一轮弹窗外点击关闭修复及其 18 项测试完整保留。

## 真实接口场景

隔离地址 `http://127.0.0.1:18094`，专用数据文件位于 `artifacts/rulebook-2-20260911/http-runtime/data/`，仅使用脚本生成的假身份；未向线上写入测试房间。

| 场景 | 结果 |
| --- | --- |
| 门清平和自摸 | 平和 1＋门清 2＋不求人 1＝4；支付后 18/6/6 |
| 同牌型点和 | 3 番不足起和，拒绝且牌局继续 |
| 副露清一色平和点和 | 4 番；应付 12、余额仅 10 实付 10，20/10/0 终场 |
| 旧断幺含刻子牌自摸 | 新版 3 番，拒绝且牌局继续 |
| 副露清全带幺自摸 | 新版 3 番，拒绝且牌局继续 |
| 副露字一色自摸 | 仅字一色 6 番；22/4/4，不叠清一色 |
| 风龙自摸 | 风龙 3＋不求人 1＝4；18/6/6 |

全部场景从 13 张等效听牌经真实摸/打进入响应；比对听牌番数、可和标记、已知牌变化后的剩余张数。合法和牌校验三座结算相同、赢家完整牌及和牌张、实际支付、三座牌谱与终帧相同；各步骤校验 108 张牌数量、可见实体唯一、每种最多 4 张、30 点守恒及他家手牌隐藏。

复现脚本：`scripts/yaoming-rulebook2-smoke.mjs`。`--seed` 以 `wx` 创建专用文件，拒绝覆盖旧测试数据；验证只允许上述回环端口且拒绝重定向。再次测试需另行保存原测试数据并停掉其服务，不能直接覆盖运行中数据。

## 浏览器验收

内置浏览器访问本机**生产构建** `http://127.0.0.1:18095/`，代理至隔离后端。

- 大厅显示已连接，打开规则手册成功。
- 界面显示规则集(2)修订说明、20 个番种、平和 1/门清 2/清全带幺 3，以及字一色不叠清一色。
- 搜索“清全带幺”只显示对应 3 番牌卡，截图检查无截字/重叠；窄窗口下牌池正常换行。
- 搜索“断幺”显示“没有找到对应番种”。

BrowserAct CLI 及 uv 不可用，未安装软件，采用已有内置浏览器执行验收。DOCX 渲染所需的捆绑 LibreOffice 缺失，未完成分页图像渲染；全文和全部表格已通过 DOCX XML 读取，本次未修改或交付 DOCX。

## 发布与恢复

发布状态：**已于 2026-09-11 13:44（UTC+8）完成前后端发布及公网核验**。

既有服务器项目 `/home/leo/mahjong_20260822`，静态站点 `/var/www/mahjong-yaoming-20260907`。不改 FRP 转发、不开放新端口，不删除房间或牌谱。发布前检测到 4 个房间均为 MATCH_END；脚本会再次核对，无进行中对局才继续。

发布脚本：`scripts/deploy-rulebook2.sh`。源包白名单校验且禁止链接；固定路径与旧产物哈希保护；备份旧源码、静态资源、JAR、停服快照；入口关闭期间恢复并核对新版指纹、版本、房间、手牌、分数、旧结算和牌谱，再开放访问。回滚必须先确认写入进程完全停止，再同时恢复旧 JAR 和旧数据。

实际发布结果：

- 检查 nginx 仅配置本站、conf.d 无其他站点。短暂停止入口和后端，完成更新后恢复；未修改 nginx 配置和 FRP 服务。
- 脚本退出码 0；4 个原房间均恢复，规则指纹写入，每房间版本只加 1；逐项核对旧结算、全部旧牌谱、归档牌谱、手牌、牌墙、分数均与停服备份相同。
- 恢复备份：`/home/leo/mahjong_20260822/backups/rulebook2-20260911/`，包含旧 JAR、源码、公开静态文件、停服后的 `backend/data`；不删除任何房间、牌谱或旧哈希资源。
- 后端 `MainPID=1521168`、`active/running`、`NRestarts=0`；nginx 和 frpc_tencent 均 active。
- 公网 `http://82.156.207.98:5173/`、新版 JS、CSS、牌面 SVG 全部 HTTP 200，SHA256 与本机测试产物一致；公网规则 API 为 20 项、平和 1/门清 2/清全带幺 3、无断幺、清一色不含字牌，仍无振听。公网房间列表为原 4 个房间。
- 公网浏览器导航尝试 15 秒后自动化超时，未据此声称公网视觉验收通过；本机生产构建视觉/交互检查和公网真实 HTTP/完整资源哈希比对已通过。建议已打开旧页面的玩家刷新一次以载入新 JS。

产物 SHA256：

- JAR：`0f9761d02b5bd45e6f83eeae5afa9a22f6127abc659bf2addfccb881bfe354d4`
- 首页：`74ff889919af47d0b2e3462488e4af82d76cead5b0e890d42acf95f1459c958a`
- JS `index-BIzaN_C2.js`：`10acdec93aef60a70973acd615815b797e687a949a2e493241712de0d6610508`
- 牌面图集保持原图案不变，250050 字节；SHA256 `4e4337f4aa7f9b0ed43134007c10e4cdeb7b42da66ffbd4ff8457af49433ef0f`。

发布源包 SHA256：`0793292205d6cc9f95f22ee27ca58d824bb04613954bdc31173a109733fe315b`。本报告的发布结果部分在发布后补全，单独同步服务器，不改变已测试的应用产物。

原始测试与构建日志保存在 `artifacts/rulebook-2-20260911/`（不加入 Git）：`backend-package.log`、`frontend-test.log`、`frontend-build.log`、`http-smoke.log`、`http-server.log`、`document-render.log`、`public-verification.log`、`deploy.log`。历史测试报告保留当时结果，未改写。
