# Dell 重装后重新部署计划

日期：2026-09-14。用户已要求重新部署，并修复 SSH 主机信任问题。

## 已确认状态

- SSH `dell` 登录成功，用户 `leo`，服务器 `leo-OptiPlex-5060`，Ubuntu 26.04.1 LTS。
- 原项目 `/home/leo/mahjong_20260822` 与静态目录不存在；PATH 未发现 Java、Node、Maven 或 Nginx。
- `frpc_tencent` 已运行；后端与 Nginx 未运行。管理员操作需要 sudo 验证。
- 本地工作区包含之前已交付功能及尚未提交的修改，必须保留并对当前源码重新验证，不用 Git 重置覆盖。

## 范围

恢复现有 Vue 正式构建和 Java 服务，不增加游戏规则或训练 AI 功能，不迁移到其他托管平台。复用当前 FRP 连接，不泄露或重写其认证配置，不对公网直接开放 Java 后端。

## 实施步骤

1. 只读检查实际端口、FRP 配置位置、服务器目录及任何可恢复的麻将数据。管理员验证失败则停止相关操作并请求用户帮助。
2. 并行运行本地前端全量测试/正式构建、后端 Maven 测试/打包。失败先定位；如需超出部署兼容性范围的功能修改则报告。
3. 使用明确白名单创建部署包：游戏源码、配置、文档、已验证 JAR 和 Vue 构建，不包含训练包、密钥、依赖缓存或本机测试房间数据。记录校验值。
4. 在服务器安装必要 Java 21+ 运行时与 Nginx。构建在本地完成，服务器不必安装 Node/Maven。还原原路径布局，先备份任何冲突文件，未发现原数据时新站点从空数据开始。
5. 配置后端 systemd（`leo` 用户、127.0.0.1:8080）与 Nginx（Vue 静态站点、同源 /api、SSE 不缓存、FRP 指向本地 5173），检查后启用服务。不得影响其他网站或改动无关 FRP 隧道。
6. 验证本机/公网 HTML、静态资源、规则 API、创建/加入房间、三四人规则、机器人、动作推进与 SSE；测试数据使用明确前缀并清理仅本次创建的测试房间。
7. 在浏览器实际打开公网网站检查页面与交互，记录测试文档、最终路径、服务状态及未验证边界。

## 恢复边界

源代码可重新部署；历史房间、牌谱必须有原数据才能恢复。不承诺恢复重装时已被删除的数据。本次允许因恢复服务而启动/重启麻将相关服务，但不删除用户文件或无关服务。
