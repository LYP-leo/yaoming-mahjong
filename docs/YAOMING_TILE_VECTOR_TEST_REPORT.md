# 原创矢量牌面重绘测试报告 · 2026-09-08

## 范围

根据用户更正及编码前 [重绘计划](YAOMING_TILE_VECTOR_PLAN.md)，图片只用于观察牌面结构，不作为运行时贴图。当前 34 种牌全部由 SVG 圆环、竹节、幺鸡、折线和手写笔画路径组成，万子及字牌也不依赖字体。参考 PNG 留存作设计资料，没有导入或打入生产包。没有修改 Java、规则、计番、通信协议或房间状态。

牌面内不显示说明用阿拉伯数字、风牌字母、Wh 或“赤”徽章。麻将自身的中文数字、萬、東南西北、中發与白板属于真实图案，继续保留。红五通过主体图案变红区分，无额外角标。

## 自动化

- `src/tileArt.test.ts`：92 项，覆盖 34 种牌、7 筒/7 条精确排列与有效颜色、花形一筒、幺鸡、折线八条、红五、无可见文字与位图、非法牌兜底、暗牌隐私和唯一渐变 ID。
- `src/tileGlyphs.test.ts`：22 项，覆盖各字形路径、万子数值、字牌色彩、无字体/图片/文字及无效输入。
- `src/yaoming/downstream-vector.test.ts`：5 项，挂载真实牌桌、结算、复盘、规则与听牌组件，验证共用 SVG、手牌隐私、和牌实体不重复、结算暗杠翻开、复盘换帧及不变更原始数据。
- `src/yaoming/vectorPresentation.test.ts`：4 项，验证贴合 SVG 轮廓的阴影、牌外红牌数量、张数编辑后的限制与原样配置提交、只读规则详情。
- 全量覆盖仍包含自动摸牌、结算、房间同步、实时推送、听牌、复盘和旧版 store。测试与构建最终结果见下方发布记录。
- 本次仅改前端，没有重新运行 Java 测试；上次 364 项后端测试的历史记录见第三批报告，不能视作本轮新运行结果。

## 浏览器验收

通过应用内浏览器，使用实际 Vue 组件，不注入牌局或读取/修改真实玩家身份：

1. 本地 `tile-art-preview.html` 逐张目检 34 种牌；筒子圆环、条子竹节/幺鸡/折线、万子手写笔画、风牌、中發白显示正常，无说明角标。
2. 七筒：顶部三个绿筒斜排，下方红筒两行两列。七条：顶部一枚红竹，下方两行各三枚，左右绿、中间深墨。91×134、43×61、24×34 三种容器均可辨认。
3. 普通五与红五并排核对，红五为同样图案的红色版本；没有额外徽章。
4. 吃、碰、明杠横置，加杠叠放、暗杠对手视角两张背面、结算视角四张亮牌以及选中手牌均使用真实组件检查。
5. 默认桌面与 390px/320px 窄屏验收，无页面横向溢出。窄屏含滚动条的有效宽度分别约 375/305px，与 scrollWidth 相等。
6. 正式应用“规则手册”加载 27 个实际用牌 SVG，无 `image` 或 SVG `text`，390px 下牌目录正常换行；控制台未见警告或错误。
7. 首次目检发现透明 SVG 外框仍沿用旧盒阴影，大牌底部多出一条横线；另外旧版选牌页仍把红牌数量叠在牌面上。两项均纳入本次修正和回归，不把首次目检计作最终验收。
8. 修正后重新截图确认大小牌底部无悬空横线，手牌选中金色轮廓与暗牌仍正常。真实 `TileSetEditor` 的三种红五数量位于牌体下方（DOM 几何断言全部通过），牌体没有重复底板或角标，编辑页图案清晰。

浏览器专用技能的命令依赖不可用，实际验收使用现有应用内浏览器完成。页面数字名称/操作说明位于牌外，ARIA/title 保留可访问牌名；这些不是绘制到牌面上的标识。

## 发布记录

- 发布前在服务器发现正在进行的真人牌局，只更新 Vue 静态前端，不重启后端、不改 FRP/Nginx、不修改或清理用户房间。
- 备份：`dell:/home/leo/mahjong_20260822/backups/yaoming-vector-20260908/before-vector.tar.gz` 与 `static-before-vector.tar.gz`，分别保存前端源文件/构建/原文档及原静态目录。
- 本地最终 `npm test`：14 个文件、312 项全部通过（20:45）。`npm run build`（vue-tsc + Vite）成功；仅生成 HTML、JS 和 CSS，没有 PNG 图集、外部牌面图片或开发验收页入口。
- 服务器最终 `npm test`：14 个文件、312 项全部通过（20:47），`npm run build` 成功。本地与服务器全部 5 个产物 SHA-256 一致。
- 发布方式：先复制新哈希资源，再原子替换静态入口；保留旧哈希文件。当前主脚本 `index-NdcqkG9B.js`，样式 `index-CADmadaI.css`。新版主脚本 SHA-256：`f697f3e4df2c6b357175cdb8959adb68e2ac2e9b02dbf79ccdd5411540ee14fa`。
- [公网首页](http://82.156.207.98:5173/) 和新主脚本 HTTP 200。应用内浏览器成功打开公网首页并进入“规则手册”，实际读取 27 个 `data-artwork="vector"` 牌面、0 个 SVG image、0 个可见 SVG text；页面主脚本为上述新版本。目检七筒、七条、万子及字牌，无数字/字母角标；控制台未见警告或错误。
- 公网 4 个新 JS/CSS 资源经 `curl.exe --fail` 下载，SHA-256 全部与本地匹配。附记：一次 PowerShell `Invoke-WebRequest` 批量抓取返回 502，因此那轮哈希结果作废；随后 curl、公网浏览器及源站检查均成功。不据此推断 502 原因或承诺链路永不波动，未修改网络服务。
- 后端发布前后 MainPID 均为 `550313`，启动时间仍为 `2026-09-07 23:18:29 CST`。没有后端重启或游戏写请求，本轮没有创建或清理任何房间。

## 边界

本轮重绘覆盖共享牌面及其呈现回归，不声称已再次穷举所有麻将玩法、网络异常或所有设备。验收专页仅用于本地开发，不包含在正式入口构建中，不连接游戏后端、不产生测试房间。原位图在源码中保留为历史设计参考；正式目录保留旧哈希资源仅供已打开旧版页面继续使用，新页面不再引用它们。
